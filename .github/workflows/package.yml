name: Package Build 
on:
  pull_request:
    types: [opened, synchronize]
  push:
    tags:
      - 'v*' 
  workflow_dispatch:
   
permissions:
  contents: write
  packages: write
  id-token: write
  actions: read

env:
  BCG729_PATH:  ${{ github.workspace }}/lib/bcg729
  OPENH264_PATH: ${{ github.workspace }}/lib/openh264
  OPENSSL_PATH: ${{ github.workspace }}/lib/openssl
  OPUS_PATH: ${{ github.workspace }}/lib/opus
  PJSIP_PATH: ${{ github.workspace }}/lib/pjsip
  MIN_IOS_VERSION: 15.0
  DOTNET_NOLOGO: true                     # Disable the .NET logo
  DOTNET_SKIP_FIRST_TIME_EXPERIENCE: true # Disable the .NET first time experience
  DOTNET_CLI_TELEMETRY_OPTOUT: true       # Disable sending .NET CLI telemetry
  PJSIP_BUILD_OUT_PATH: ${{ github.workspace }}/outputs/pjsip-build-output
  SWIG_BUILD_OUT_PATH: ${{ github.workspace }}/outputs/swig-build-output    
  PJSIP_VERSION: 2.17
  OPENSSL_VERSION: OpenSSL_1_1_1s
  OPENH264_VERSION: v2.3.0
  OPUS_VERSION: v1.4
  BCG729_VERSION: 1.1.1
  MIN_MACOSX_VERSION: 15.0
  SDKVERSION: 26.2
  BUILD_TYPE: Release
  NUGET_ARTIFACTS: ${{ github.workspace }}/artifacts
  ARTIFACTS: /tmp/artifacts
  ROOT_DIR: ${{ github.workspace }}
  LOGS_DIR: ${{ github.workspace }}\logs
  INSTALL_DIR: ${{ github.workspace }}\install

jobs:
  bcg729-android:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      
      - name: Cache BCG729 Built SDK
        id: cache-libs
        uses: actions/cache@v5
        with:
          path: ${{ github.workspace }}/bcg729-output
          key: bcg729-${{ env.BCG729_VERSION }}-android
    
      - name: Create Output Directory
        if: steps.cache-libs.outputs.cache-hit != 'true'
        run: mkdir -p ${{ github.workspace }}/bcg729-output

      - name: Set Permissions for Output Directory
        if: steps.cache-libs.outputs.cache-hit != 'true'
        run: chmod -R 777 ${{ github.workspace }}/bcg729-output

      - name: Build Docker Image
        if: steps.cache-libs.outputs.cache-hit != 'true'
        run: |
          docker build -t bcg729-builder ${{ github.workspace }}/res/docker/bcg729
        
      - name: Run Docker Container 
        if: steps.cache-libs.outputs.cache-hit != 'true'
        run: |
          docker run --name bcg729-build bcg729-builder
          
      - name: Extract Artifacts from Container
        if: steps.cache-libs.outputs.cache-hit != 'true'
        run: |
          docker cp bcg729-build:/root/outputs/libs ${{ github.workspace }}/bcg729-output
 
      - name: Upload bcg729 Artifacts
        uses: actions/upload-artifact@v7
        with:
          name: bcg729-android
          path: ./bcg729-output 
          overwrite: true
      
  openh264-android:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
  
      - name: Cache H264 Built SDK
        id: cache-libs
        uses: actions/cache@v5
        with:
          path: ${{ github.workspace }}/libopenh264-output
          key: h264-${{ env.OPENH264_VERSION }}-android
    
      - name: Create Output Directory
        run: mkdir -p ${{ github.workspace }}/libopenh264-output
        if: steps.cache-libs.outputs.cache-hit != 'true'

      - name: Set Permissions for Output Directory
        run: chmod -R 777 ${{ github.workspace }}/libopenh264-output
        if: steps.cache-libs.outputs.cache-hit != 'true'

      - name: Build Docker Image
        if: steps.cache-libs.outputs.cache-hit != 'true'
        run: |
          docker build -t openh264-builder ${{ github.workspace }}/res/docker/openh264

      - name: Run Docker Container 
        if: steps.cache-libs.outputs.cache-hit != 'true'
        run: | 
          docker run --name openh264-build openh264-builder
          
      - name: Extract Artifacts from Container
        if: steps.cache-libs.outputs.cache-hit != 'true'
        run: | 
          docker cp openh264-build:/root/outputs/libs ${{ github.workspace }}/libopenh264-output
           
      - name: Upload OpenH264 Artifacts
        uses: actions/upload-artifact@v7
        with:
          name: openh264-android
          path: ${{ github.workspace }}/libopenh264-output
          overwrite: true
   
  openssl-android:
    runs-on: ubuntu-latest
    
    steps:
        
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Cache SSL Built SDK
        id: cache-libs
        uses: actions/cache@v5
        with:
          path: ${{ github.workspace }}/openssl-output
          key: ssl-${{ env.OPENSSL_VERSION }}-android
    
      - name: Create Output Directory
        run: mkdir -p ${{ github.workspace }}/openssl-output
        if: steps.cache-libs.outputs.cache-hit != 'true'

      - name: Set Permissions for Output Directory
        run: chmod -R 777 ${{ github.workspace }}/openssl-output
        if: steps.cache-libs.outputs.cache-hit != 'true'

      - name: Build Docker Image
        run: |
          docker build -t openssl-builder ${{ github.workspace }}/res/docker/openssl
        if: steps.cache-libs.outputs.cache-hit != 'true'
        
      - name: Run Docker Container and Extract Artifacts
        run: |  
          docker run --name openssl-build openssl-builder
        if: steps.cache-libs.outputs.cache-hit != 'true'
      
      - name: Extract Artifacts from Container
        run: |  
          docker cp openssl-build:/root/outputs/libs ${{ github.workspace }}/openssl-output
        if: steps.cache-libs.outputs.cache-hit != 'true'
          

      - name: Upload openssl Artifacts
        uses: actions/upload-artifact@v7
        with:
          name: openssl-android
          path: ./openssl-output
          overwrite: true

  opus-android:
    runs-on: ubuntu-latest   
    steps:  
      - name: Checkout repository
        uses: actions/checkout@v6
        
      - name: Cache opus Built SDK
        id: cache-libs
        uses: actions/cache@v5
        with:
          path:  ${{ github.workspace }}/opus-output
          key: opus-${{ env.OPUS_VERSION }}-android

      - name: Clone Opus
        if: steps.cache-libs.outputs.cache-hit != 'true'
        run: |
          mkdir -p ${{ github.workspace }}/lib
          cd ${{ github.workspace }}/lib          
          git clone --branch v1.4 https://github.com/xiph/opus.git opus

      - name: Create Output Directory
        run: mkdir -p ${{ github.workspace }}/opus-output
        if: steps.cache-libs.outputs.cache-hit != 'true'

      - name: Build Docker Image
        if: steps.cache-libs.outputs.cache-hit != 'true'
        run: |
          docker build -t opus-builder ${{ github.workspace }}/res/docker/opus
          
      - name: Run Docker Container
        if: steps.cache-libs.outputs.cache-hit != 'true'
        run: docker run --name opus-build opus-builder
        
      - name: Extract Artifacts from Container
        if: steps.cache-libs.outputs.cache-hit != 'true'
        run: | 
          docker cp opus-build:/root/outputs/ ${{ github.workspace }}/opus-output

      - name: Upload opus Artifacts
        uses: actions/upload-artifact@v7
        with:
          name: opus-android
          path: ${{ github.workspace }}/opus-output 
          overwrite: true
        
  pjsip-android:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        arch: [x86, x86_64, armeabi-v7a, arm64-v8a]
      fail-fast: false
    needs: [bcg729-android, openh264-android, openssl-android, opus-android]
    name: pjsip - android
    permissions:
      contents: write

    steps: 
    - uses: actions/checkout@v6 
  
    - name: Cache opus Built SDK
      id: cache-libs
      uses: actions/cache@v5
      with:
        path: ${{ env.PJSIP_BUILD_OUT_PATH }}/pjsip-${{ env.PJSIP_VERSION }}-android-${{matrix.arch}}
        key: pjsip-${{ env.PJSIP_VERSION }}-android-${{ matrix.arch }}

    - name: Clone PJSIP
      if: steps.cache-libs.outputs.cache-hit != 'true'
      run: |
        mkdir -p ${{ github.workspace }}/lib
        cd ${{ github.workspace }}/lib
        git clone --branch $PJSIP_VERSION https://github.com/pjsip/pjproject.git pjsip
     
    - uses: actions/setup-java@v4
      if: steps.cache-libs.outputs.cache-hit != 'true'
      with:
        distribution: 'temurin'
        java-version: '17'
        check-latest: true
      
    - name: Setup Android SDK
      if: steps.cache-libs.outputs.cache-hit != 'true'
      uses: android-actions/setup-android@v3
      with:
        cmdline-tools-version: 11076708 
        packages: platforms;android-28 tools platform-tools build-tools;34.0.0
    
    - uses: nttld/setup-ndk@v1
      if: steps.cache-libs.outputs.cache-hit != 'true'
      id: setup-ndk
      with:
        ndk-version: r28
        add-to-path: true
        link-to-sdk: true
      
    - name: Debug NDK
      if: steps.cache-libs.outputs.cache-hit != 'true'
      shell: bash
      run: |
        ls -R $ANDROID_NDK_HOME
      env: 
        ANDROID_NDK_HOME: ${{ steps.setup-ndk.outputs.ndk-path }}

    - name: Prepare Environment
      if: steps.cache-libs.outputs.cache-hit != 'true'
      run: |
        cd ${{ github.workspace }}
        sudo mkdir -p /home/runner/libpjsua2 
        
        DEPS="unzip git curl bzip2 binutils make autoconf openssl \
          libssl-dev libopus0 libpcre3 libpcre3-dev build-essential nasm python3 swig libstdc++6 \
          libpthread-stubs0-dev \
          libc6-dev"
        sudo sh -c "dpkg --add-architecture i386; apt-get update  && apt-get install -y ${DEPS}"
    
    - uses: actions/download-artifact@v8
      if: steps.cache-libs.outputs.cache-hit != 'true'
      with:
        name: openssl-android
        path: ${{ github.workspace }}/output/openssl
      
    - uses: actions/download-artifact@v8
      if: steps.cache-libs.outputs.cache-hit != 'true'
      with:
        name: openh264-android
        path: ${{ github.workspace }}/output/openh264/
        
    - uses: actions/download-artifact@v8
      if: steps.cache-libs.outputs.cache-hit != 'true'
      with:
        name: opus-android
        path: ${{ github.workspace }}/output/opus/
        
    - uses: actions/download-artifact@v8
      if: steps.cache-libs.outputs.cache-hit != 'true'
      with:
        name: bcg729-android
        path: ${{ github.workspace }}/output/bcg729/
       
    - name: Create config_site.h
      if: steps.cache-libs.outputs.cache-hit != 'true'
      run: |
        echo "Creating config site file for Android ..."
        echo "#define PJ_CONFIG_ANDROID                 1"                       > ${{ env.CONFIG_SITE_PATH }}
        echo "#define PJMEDIA_HAS_G7221_CODEC           1"                       >> ${{ env.CONFIG_SITE_PATH }}
        echo "#define PJMEDIA_AUDIO_DEV_HAS_ANDROID_JNI 0"                       >> ${{ env.CONFIG_SITE_PATH }}
        echo "#define PJMEDIA_AUDIO_DEV_HAS_OPENSL      1"                       >> ${{ env.CONFIG_SITE_PATH }}
        echo "#define PJSIP_AUTH_AUTO_SEND_NEXT         0"                       >> ${{ env.CONFIG_SITE_PATH }}
        echo "Changing PJSIP_MAX_PKT_LEN to 6000"
        echo "#define PJSIP_MAX_PKT_LEN                 6000"                    >> ${{ env.CONFIG_SITE_PATH }}
        echo "Enabling BCG729 in config_site"
        echo "#define PJMEDIA_HAS_BCG729                1"                       >> ${{ env.CONFIG_SITE_PATH }}
        echo "Enabling h264 and Video support in config_site"
        echo "#define PJMEDIA_HAS_VIDEO                 1"                       >> ${{ env.CONFIG_SITE_PATH }}
        echo "#define PJMEDIA_HAS_OPENH264_CODEC        1"                       >> ${{ env.CONFIG_SITE_PATH }}
        echo "Enabling Opus in config_site"
        echo "#define PJMEDIA_HAS_OPUS_CODEC            1"                       >> ${{ env.CONFIG_SITE_PATH }} 
        echo "Enabling SSL in config_site"
        echo "#define PJ_HAS_SSL_SOCK                   1"                       >> ${{ env.CONFIG_SITE_PATH }}
        echo "#define PJ_SSL_SOCK_IMP                   PJ_SSL_SOCK_IMP_OPENSSL" >> ${{ env.CONFIG_SITE_PATH }}
        echo "#include <pj/config_site_sample.h>"                                >> ${{ env.CONFIG_SITE_PATH }}
        cp -v ${{ github.workspace }}/res/pj/pjsua2maui/Makefile ${{ env.PJSIP_PATH }}/pjsip-apps/src/swig/csharp/pjsua2maui

    - name: Build
      if: steps.cache-libs.outputs.cache-hit != 'true'
      id: build-pj
      run: |
        cd ${{ env.PJSIP_PATH }}
        mkdir -p ${{ env.PJSIP_TMP_PATH }} 
        cp -r * ${{ env.PJSIP_TMP_PATH }}
        cd  ${{ env.PJSIP_TMP_PATH }}
        echo "Configuring for ${{ matrix.arch }}"
        APP_PLATFORM=android-${{ env.TARGET_ANDROID_API }} TARGET_ABI=${{ matrix.arch }} ./configure-android --use-ndk-cflags --with-ssl=${{ env.OPENSSL_BUILD_OUT_PATH }}/libs/${{ matrix.arch }} --with-openh264=${{ env.OPENH264_BUILD_OUT_PATH }}/libs/${{ matrix.arch }} --with-opus=${{ env.OPUS_BUILD_OUT_PATH }}/${{ matrix.arch }} --with-bcg729=${{ env.BCG729_BUILD_OUT_PATH }}/libs/${{ matrix.arch}}
        echo "Make dep"
        make dep
        echo ""
        echo "make"
        make
        cd ${{ env.PJSUA_BASE_DIR }}/csharp/pjsua2maui
        make

        mkdir -p ${{ env.PJSIP_BUILD_OUT_PATH }}/pjsip-${{ env.PJSIP_VERSION }}-android-${{matrix.arch}}/lib
        mkdir -p ${{ env.PJSIP_BUILD_OUT_PATH }}/pjsip-${{ env.PJSIP_VERSION }}-android-${{matrix.arch}}/java
        mkdir -p ${{ env.PJSIP_BUILD_OUT_PATH }}/pjsip-${{ env.PJSIP_VERSION }}-android-${{matrix.arch}}/csharp
        shopt -s globstar && cp ${{ github.workspace }}/output/**/libs/${{ matrix.arch }}/**/*.so ${{ env.PJSIP_BUILD_OUT_PATH }}/pjsip-${{ env.PJSIP_VERSION }}-android-${{matrix.arch}}/lib
        cp -v ${{ env.PJSUA_GENERATED_SO_DIR }}/${{ matrix.arch }}/*.so ${{ env.PJSIP_BUILD_OUT_PATH }}/pjsip-${{ env.PJSIP_VERSION }}-android-${{matrix.arch}}/lib/ 
        cp -v ${{ env.PJSUA_BASE_DIR }}/csharp/pjsua2maui/pjsua2maui/Platforms/Android/org/pjsip/*.java ${{ env.PJSIP_BUILD_OUT_PATH }}/pjsip-${{ env.PJSIP_VERSION }}-android-${{matrix.arch}}/java
        cp -v ${{ env.PJSUA_GENERATED_CSHARP_DIR }}/*.cs ${{ env.PJSIP_BUILD_OUT_PATH }}/pjsip-${{ env.PJSIP_VERSION }}-android-${{matrix.arch}}/csharp
         
      env:
        ANDROID_NDK_ROOT: ${{ steps.setup-ndk.outputs.ndk-path }}

    - name: Upload pjsip artifacts
      uses: actions/upload-artifact@v7
      with:
        name: pjsip-${{ env.PJSIP_VERSION }}-android-${{ matrix.arch }}
        path: ${{ env.PJSIP_BUILD_OUT_PATH }}/pjsip-${{ env.PJSIP_VERSION }}-android-${{matrix.arch}}
 
    env:
      OPENSSL_LIB_PATH: ${{ github.workspace }}/output/openssl/libs/
      OPENh264_LIB_PATH: ${{ github.workspace }}/output/openh264/libs/
      OPUS_LIB_PATH: ${{ github.workspace }}/output/opus/libs/
      BCG729_LIB_PATH: ${{ github.workspace }}/output/bcg729/libs/
      PJSIP_TMP_PATH: /tmp/pjsip
      CONFIG_SITE_PATH: ${{ github.workspace }}/lib/pjsip/pjlib/include/pj/config_site.h
      PJSUA_GENERATED_CSHARP_DIR: /tmp/pjsip/pjsip-apps/src/swig/csharp/pjsua2maui/pjsua2maui/Platforms/Android/pjsua2 
      PJSUA_GENERATED_SO_DIR: /tmp/pjsip/pjsip-apps/src/swig/csharp/pjsua2maui/pjsua2maui/Platforms/Android/lib
      PJSUA_BASE_DIR: /tmp/pjsip/pjsip-apps/src/swig
      TARGET_ANDROID_API: 28
      OPENSSL_BUILD_OUT_PATH: ${{ github.workspace }}/output/openssl
      OPENH264_BUILD_OUT_PATH: ${{ github.workspace}}/output/openh264
      OPUS_BUILD_OUT_PATH: ${{ github.workspace }}/output/opus/outputs
      BCG729_BUILD_OUT_PATH: ${{ github.workspace }}/output/bcg729

  bcg729-ios:
    strategy:
      fail-fast: false
      matrix:
        arch: [x86_64, arm64, arm64s]
        include: 
          - lib_name: bcg729
          - arch: x86_64
            platform: iossimulator
            os_platform: iPhoneSimulator
          - arch: arm64s
            platform: iossimulator
            os_platform: iPhoneSimulator
          - arch: arm64
            platform: ios
            os_platform: iPhoneOS
    runs-on: macos-15
    name: bcg729-ios 
    permissions:
      contents: write
    steps:       
  
    - name: Cache BCG729 Built SDK
      id: cache-libs
      uses: actions/cache@v5
      with:
        path: ${{ github.workspace }}/${{ matrix.os_platform }}${{ env.MIN_IOS_VERSION }}-${{ matrix.arch }}.sdk
        key: ${{ matrix.lib_name }}-${{ env.BCG729_VERSION }}-${{ matrix.platform }}-${{ env.SDKVERSION }}-${{ matrix.arch }}
  
    - name: Clone BCG729
      if: steps.cache-libs.outputs.cache-hit != 'true'
      run: |
        mkdir -p ${{ github.workspace }}/lib/
        cd ${{ github.workspace }}/lib/
        git clone --branch $BCG729_VERSION https://github.com/BelledonneCommunications/bcg729.git bcg729 
        
    - name: Setup Xcode 26.4
      uses: maxim-lobanov/setup-xcode@v1
      if: steps.cache-libs.outputs.cache-hit != 'true'
      with:
        xcode-version: '26.3'
 
    - name: Install deps
      if: steps.cache-libs.outputs.cache-hit != 'true'
      run: |
        brew install nasm autoconf automake libtool
      shell: sh

    - name: Build bcg729 for iOS
      id: build
      if: steps.cache-libs.outputs.cache-hit != 'true'
      run: |
        echo "Building BCG729 for iOS"
        xcode_dev_path=$(xcode-select -print-path)
        cd ${{ env.BCG729_PATH }}

        echo "-- Compiling for ${{ matrix.arch }}"

        case "${{ matrix.arch }}" in
          x86_64)
            os_platform="iPhoneSimulator"
            cmake_arch="x86_64"
            ;;
          arm64)
            os_platform="iPhoneOS"
            cmake_arch="arm64"
            ;;
          arm64s)
            os_platform="iPhoneSimulator"
            cmake_arch="arm64"
            ;;
        esac

        sdk_path="${xcode_dev_path}/Platforms/${{ matrix.os_platform }}.platform/Developer/SDKs/${{ matrix.os_platform }}.sdk"

        build_dir="${{ github.workspace }}/${{ matrix.os_platform }}${{ env.MIN_IOS_VERSION }}-${{ matrix.arch }}.sdk"
        rm -rf "$build_dir" CMakeFiles CMakeCache.txt

        mkdir -p "$build_dir"

        cmake \
          -DCMAKE_POLICY_VERSION_MINIMUM=3.5 \
          -DCMAKE_OSX_ARCHITECTURES=$cmake_arch \
          -DCMAKE_BUILD_TYPE=Release \
          -DCMAKE_INSTALL_PREFIX="$build_dir" \
          -DCMAKE_SKIP_INSTALL_RPATH=ON \
          -DCMAKE_MAKE_PROGRAM:PATH=$(which make) \
          -DCMAKE_C_COMPILER=$(which clang) \
          -DCMAKE_OSX_SYSROOT="$sdk_path" \
          .

        make || exit 1
        make install

    - name: Upload artifacts
      uses: actions/upload-artifact@v7
      with:
        name: bcg729-${{ env.BCG729_VERSION }}-${{ matrix.os_platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
        path: ${{ github.workspace }}/${{ matrix.os_platform }}${{ env.MIN_IOS_VERSION }}-${{ matrix.arch }}.sdk
        overwrite: true
   
  openh264-ios:
    strategy:
      fail-fast: false
      matrix:
        arch: [x86_64, arm64, arm64s]
        include:
          - arch: arm64
            resolved_arch: arm64
            platform: iPhoneOS
          - arch: arm64s
            resolved_arch: arm64
            platform: iPhoneSimulator
          - arch: x86_64
            resolved_arch: x86_64
            platform: iPhoneSimulator
    runs-on: macos-15
    name: openh264-ios
    permissions:
      contents: write
    steps:
         
    - uses: actions/checkout@v6
      
    - name: Cache H264 Built SDK
      id: cache-libs
      uses: actions/cache@v5
      with:
        path: ${{ env. BUILT_SDK_PATH }}
        key: h264-${{ env.OPENH264_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
      env: 
        BUILT_SDK_PATH: ${{ github.workspace }}/h264-${{ env.OPENH264_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
        
    - name: Clone OpenH264
      if: steps.cache-libs.outputs.cache-hit != 'true'
      run: |
        mkdir -p ${{ github.workspace }}/lib
        cd ${{ github.workspace }}/lib
        git clone --branch v2.3.0 https://github.com/cisco/openh264.git
 
    - name: Setup Xcode 26.3
      uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: '26.3'
      if: steps.cache-libs.outputs.cache-hit != 'true'
        
    - name: Install deps
      run: |
        brew install nasm autoconf automake libtool 
      shell: sh
      if: steps.cache-libs.outputs.cache-hit != 'true'
          
    - name: Build OpenH264 for ${{ matrix.arch }}
      id: build
      if: steps.cache-libs.outputs.cache-hit != 'true'
      run: |
        cd ${{ env.OPENH264_PATH }}
        h264_makefile="Makefile"
        h264_makefile_bak="Makefile.bak"

        case "${{ matrix.arch }}" in
          x86_64)
            platform="iPhoneSimulator"
            sdk="iphonesimulator"
            os="darwin"
            version_min="-mios-simulator-version-min=${{ env.MIN_IOS_VERSION }}"
            ;;
          arm64s)
            platform="iPhoneSimulator"
            sdk="iphonesimulator"
            os="darwin"
            version_min="-mios-simulator-version-min=${{ env.MIN_IOS_VERSION }}"
            ;;
          arm64)
            platform="iPhoneOS"
            sdk="iphoneos"
            os="ios"
            version_min="-miphoneos-version-min=${{ env.MIN_IOS_VERSION }}"
            ;;
        esac

        sdk_path=$(xcrun --sdk $sdk --show-sdk-path)
        developer_path=$(xcode-select -p)

        h264_prefix="${{ env.BUILT_SDK_PATH }}"
        h264_log="${h264_prefix}/build.log"
        mkdir -p "${h264_prefix}/logs"

        echo "Building OpenH264 for ${{ matrix.arch }} ($platform, OS=$os, SDK=$sdk)"

        cp "${h264_makefile}" "${h264_makefile_bak}"
        sed -i.deleteme "s|^PREFIX=.*|PREFIX=${h264_prefix}|" "${h264_makefile}"
        rm -f ${h264_makefile}.deleteme

        export CFLAGS="-arch ${{ matrix.resolved_arch }} ${version_min} -isysroot ${sdk_path}"
        export LDFLAGS="-arch ${{ matrix.resolved_arch }} ${version_min} -isysroot ${sdk_path}"
        export SYSROOT="${sdk_path}"

        make OS=${os} ARCH=${{ matrix.resolved_arch }} \
            SDK_MIN=${{ env.MIN_IOS_VERSION }} v=No >> "${h264_log}" || exit 1

        make OS=${os} ARCH=${{ matrix.resolved_arch }} \
            SDK_MIN=${{ env.MIN_IOS_VERSION }} v=No install >> "${h264_log}" || exit 1

        make OS=${os} ARCH=${{ matrix.resolved_arch }} \
            SDK_MIN=${{ env.MIN_IOS_VERSION }} v=No clean >> "${h264_log}" || exit 1

        mv "${h264_makefile_bak}" "${h264_makefile}"
 
      shell: bash
      env: 
        BUILT_SDK_PATH: ${{ github.workspace }}/h264-${{ env.OPENH264_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
        
    - name: Upload artifacts
      uses: actions/upload-artifact@v7
      with:
        name: h264-${{ env.OPENH264_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
        path: ${{ github.workspace }}/h264-${{ env.OPENH264_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
        overwrite: true

  openssl-ios:
    strategy:
      fail-fast: false
      matrix:
        arch: [x86_64, arm64, arm64s]
        include:
          - arch: x86_64
            resolved_arch: x86_64
            sdk: iphonesimulator
            platform: iPhoneSimulator
            target: ios-sim-cross-x86_64
          - arch: arm64
            resolved_arch: arm64
            sdk: iphoneos
            platform: iPhoneOS
            target: ios-cross-arm64
          - arch: arm64s
            resolved_arch: arm64
            sdk: iphonesimulator
            platform: iPhoneSimulator
            target: ios-sim-cross-arm64
    runs-on: macos-15
    name: openssl-ios 
    permissions:
      contents: write
    steps:
    - uses: actions/checkout@v6 
    
    - name: Cache SSL Built SDK
      id: cache-libs
      uses: actions/cache@v5
      with:
        path: ${{ env.OPENSSL_PATH }}/bin/${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.resolved_arch }}.sdk
        key: ssl-${{ env.OPENSSL_VERSION }}-${{ matrix.platform }}

    - name: Clone OpenSSL
      if: steps.cache-libs.outputs.cache-hit != 'true'
      run: |
        mkdir -p ${{ github.workspace }}/lib/
        cd ${{ github.workspace }}/lib/
        git clone --branch $OPENSSL_VERSION https://github.com/openssl/openssl.git --recursive      

    - name: Setup Xcode 26.3
      uses: maxim-lobanov/setup-xcode@v1
      if: steps.cache-libs.outputs.cache-hit != 'true'
      with:
        xcode-version: '26.3'
 
    - name: Install deps
      if: steps.cache-libs.outputs.cache-hit != 'true'
      run: |
        brew install nasm autoconf automake libtool 
      shell: sh
        
    - name: Build OpenSSL
      if: steps.cache-libs.outputs.cache-hit != 'true'
      id: build
      run: |
        INCLUDE_DIR=""
        OPENSSLCONF_ALL=()
        LIBSSL_IOS=()
        LIBSSL_IOSSIM=()
        LIBCRYPTO_IOS=()
        LIBCRYPTO_IOSSIM=()
        export OPENSSL_LOCAL_CONFIG_DIR=${{ github.workspace }}/res/ssl/config
        SDKVERSION=$(xcrun -sdk ${{ matrix.sdk }} --show-sdk-version)
        export SDKVERSION
        export IOS_MIN_SDK_VERSION=15.0
        export CONFIG_DISABLE_BITCODE=true 
        export CONFIG_OPTIONS="no-dso no-shared no-engine no-tests no-unit-test no-ssl3 no-comp enable-ec_nistp_64_gcc_128"
        cd ${{ env.OPENSSL_PATH }}
        
        chmod u+x ./Configure
        
        TARGET=${{ matrix.target }}
        PLATFORM=${{ matrix.platform }}
        ARCH=${{ matrix.resolved_arch }}
        # Cross compile references, see Configurations/10-main.conf
        export CROSS_COMPILE="${{ env.DEVELOPER }}/Toolchains/XcodeDefault.xctoolchain/usr/bin/"
        export CROSS_TOP="${{ env.DEVELOPER }}/Platforms/${{ matrix.platform }}.platform/Developer"
        export CROSS_SDK="${{ matrix.platform }}${{ env.SDKVERSION }}.sdk"

        # Prepare TARGETDIR  
        TARGETDIR="${{ env.OPENSSL_PATH }}/bin/${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.resolved_arch }}.sdk"
        echo "artifact=${TARGETDIR}" >> "$GITHUB_OUTPUT"
        mkdir -p "${TARGETDIR}"
        ## Determine config options
        # Add build target, --prefix and prevent async (references to getcontext(),
        # setcontext() and makecontext() result in App Store rejections) and creation
        # of shared libraries (default since 1.1.0)
        LOCAL_CONFIG_OPTIONS="${TARGET} --prefix=${TARGETDIR} ${CONFIG_OPTIONS} no-async no-shared"

        # Only relevant for 64 bit builds
        # if [[ "${CONFIG_ENABLE_EC_NISTP_64_GCC_128}" == "true" && "${{ matrix.resolved_arch }}" == *64  ]]; then
        #     LOCAL_CONFIG_OPTIONS="${LOCAL_CONFIG_OPTIONS} enable-ec_nistp_64_gcc_128"
        # fi

        export CFLAGS="-isysroot ${CROSS_TOP}/SDKs/${CROSS_SDK} -mios-version-min=${IOS_MIN_SDK_VERSION}"
        export LDFLAGS="-isysroot ${CROSS_TOP}/SDKs/${CROSS_SDK}"

        # Run Configure
        ./Configure ${LOCAL_CONFIG_OPTIONS} no-tests 

        # Run make
        echo "  Make (using ${{ env.BUILD_THREADS }} thread(s))..." 
        make build_libs -j "${{ env.BUILD_THREADS }}"  
 
        # Run make install
        set -e 
        make install_dev  

        # Remove source dir, add references to library files to relevant arrays
        # Keep reference to first build target for include file
        if [[ "${{ matrix.platform }}" == iPhoneOS ]]; then
            LIBSSL_IOS+=("${TARGETDIR}/lib/libssl.a")
            LIBCRYPTO_IOS+=("${TARGETDIR}/lib/libcrypto.a")
            OPENSSLCONF_SUFFIX="ios_${{matrix.arch}}"
        elif [[ "${PLATFORM}" == iPhoneSimulator ]]; then
            LIBSSL_IOSSIM+=("${TARGETDIR}/lib/libssl.a")
            LIBCRYPTO_IOSSIM+=("${TARGETDIR}/lib/libcrypto.a")
            OPENSSLCONF_SUFFIX="ios_${{ matrix.arch }}"
        fi
      shell: bash
      env:
        DEVELOPER: $(xcode-select -print-path)
        BUILD_THREADS: $(sysctl -n hw.ncpu)  

    - name: Upload artifacts
      uses: actions/upload-artifact@v7
      with:
        name: openssl-${{ matrix.arch }}
        path: ${{ env.OPENSSL_PATH }}/bin/${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.resolved_arch }}.sdk
        overwrite: true 

  opus-ios:
    strategy:
      fail-fast: false
      matrix:
        arch: [x86_64, arm64, arm64s]
        include:
          - arch: arm64
            resolved_arch: arm64
            platform: iPhoneOS
          - arch: arm64s
            resolved_arch: arm64
            platform: iPhoneSimulator
          - arch: x86_64
            resolved_arch: x86_64
            platform: iPhoneSimulator
    runs-on: macos-15
    name: opus-ios 
    permissions:
      contents: write
    steps:
     
    - name: Cache opus Built SDK
      id: cache-libs
      uses: actions/cache@v5
      with:
        path: ${{ github.workspace }}/opus/build/${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.resolved_arch }}.sdk
        key: opus-${{ env.OPUS_VERSION }}-${{ matrix.platform }}-${{ env.SDKVERSION }}-${{ matrix.arch }}

    - name: Clone Opus
      if: steps.cache-libs.outputs.cache-hit != 'true'
      run: |
        mkdir -p ${{ github.workspace }}/lib
        cd ${{ github.workspace }}/lib
        git clone --branch v1.4 https://github.com/xiph/opus.git opus

    - name: Setup Xcode 26.3
      if: steps.cache-libs.outputs.cache-hit != 'true'
      uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: '26.3'
 
    - name: Install deps
      if: steps.cache-libs.outputs.cache-hit != 'true'
      run: |
        brew install nasm autoconf automake libtool 
      shell: sh
         
    - name: Build opus
      id: build
      if: steps.cache-libs.outputs.cache-hit != 'true'
      run: |
        opus_version="1.4"
        opus_opt_cflags="-O3 -ffast-math -flto -g"
        opus_opt_ldflags="-flto"
        DEVELOPER=$(xcode-select -print-path)
        SDKVERSION=$(xcrun -sdk ${{ matrix.arch == 'arm64' && 'iphoneos' || 'iphonesimulator' }} --show-sdk-version)
        PLATFORM=${{ matrix.platform }}
        TARGETDIR="${{ github.workspace }}/opus/build/${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.resolved_arch }}.sdk"

        mkdir -p "${TARGETDIR}/include"
        mkdir -p "${TARGETDIR}/lib"

        echo "artifact=${TARGETDIR}" >> "$GITHUB_OUTPUT"
        echo "-- Building Opus for ${{ matrix.arch }} on ${PLATFORM}"

        cd "${{ env.OPUS_PATH }}"

        ./autogen.sh
 
        if [[ "${{ matrix.arch }}" == "x86_64" ]]; then
          HOST="x86_64-apple-darwin" 
          opus_opt_cflags="-O3 -ffast-math -flto -g -Wno-cast-align"
        else
          HOST="arm-apple-darwin" 
          opus_opt_cflags="-O3 -ffast-math -flto -g"
        fi
        version_min=$([[ "${{ matrix.arch }}" == "arm64" ]] && echo "-miphoneos-version-min=${{ env.MIN_IOS_VERSION }}" || echo "-mios-simulator-version-min=${{ env.MIN_IOS_VERSION }}")
        ARCH="${{ matrix.resolved_arch }}"
        
        ./configure \
          --enable-float-approx \
          --disable-shared \
          --enable-static \
          --with-pic \
          --disable-extra-programs \
          --disable-doc \
          --host=${HOST} \
          --prefix="${TARGETDIR}" \
          LDFLAGS="${opus_opt_ldflags} -fPIE ${version_min} -L${TARGETDIR}/lib" \
          CFLAGS="${opus_opt_cflags} -arch ${ARCH} -fPIE ${version_min} -I${TARGETDIR}/include -isysroot ${DEVELOPER}/Platforms/${PLATFORM}.platform/Developer/SDKs/${PLATFORM}${SDKVERSION}.sdk"

        make -j${{ env.BUILD_THREADS }}
        make install 
      shell: sh
      env:
        CONFIG_DISABLE_BITCODE: true
        DEVELOPER: $(xcode-select -print-path)
        IOS_SDKVERSION: $(xcrun -sdk iphoneos --show-sdk-version)
        BUILD_THREADS: $(sysctl -n hw.ncpu)

    - name: Patch Opus .pc for arm64s
      if: matrix.arch == 'arm64s' && steps.cache-libs.outputs.cache-hit != 'true'
      run: |
        opus_build_dir=$(find "${{ github.workspace }}/opus/build/" -type d -name "iPhoneSimulator*-arm64.sdk" | head -n 1)
        pc_file=$(find "$opus_build_dir/lib/pkgconfig" -name 'opus.pc' | head -n 1)

        if [ -f "$pc_file" ]; then
          install_prefix="${{ github.workspace }}/libs/opus"
          sed -i'' -e "s|^prefix=.*|prefix=${install_prefix}|" "$pc_file"
          sed -i'' -e "s|^exec_prefix=.*|exec_prefix=${install_prefix}|" "$pc_file"
          sed -i'' -e "s|^libdir=.*|libdir=${install_prefix}/lib|" "$pc_file"
          sed -i'' -e "s|^includedir=.*|includedir=${install_prefix}/include|" "$pc_file"
          sed -i'' -e "s|^Cflags=.*|Cflags=-I${install_prefix}/include/opus|" "$pc_file"
          echo "Patched Opus pkg-config file for PJSIP:"
          cat "$pc_file"
        else
          echo "Opus .pc file not found in $opus_build_dir"
          exit 1
        fi
      shell: bash

    - name: Upload artifacts
      uses: actions/upload-artifact@v7
      with:
        name: opus-${{ matrix.arch }}
        path: ${{ github.workspace }}/opus/build/${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.resolved_arch }}.sdk
        overwrite: true

  pjsip-ios:
    strategy:
      matrix:
        arch: [x86_64, arm64, arm64s]
        include:
        - arch: arm64
          resolved_arch: arm64
          sdk: iphoneos
          platform: iPhoneOS
        - arch: arm64s
          resolved_arch: arm64
          sdk: iphonesimulator
          platform: iPhoneSimulator
        - arch: x86_64
          resolved_arch: x86_64
          sdk: iphonesimulator
          platform: iPhoneSimulator
      fail-fast: false
    runs-on: macos-15
    needs: [bcg729-ios, openh264-ios, openssl-ios, opus-ios]
    name: pjsip - ios
    permissions:
      contents: write
    steps:
      
      - uses: actions/checkout@v6
      
      - name: Cache pjsip Built SDK
        uses: actions/cache@v5
        id: cache-libs
        with:
          path: ${{ github.workspace }}/pjsip-${{matrix.arch}}
          key: pjsip-${{ env.PJSIP_VERSION }}${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}

      - name: Clone PJSIP
        if: steps.cache-libs.outputs.cache-hit != 'true'
        run: |
          mkdir -p ${{ github.workspace }}/lib
          cd ${{ github.workspace }}/lib
          git clone --branch $PJSIP_VERSION https://github.com/pjsip/pjproject.git pjsip
      
      - uses: actions/download-artifact@v8
        if: steps.cache-libs.outputs.cache-hit != 'true'
        with:
          name: openssl-${{ matrix.arch }}
          path: ${{ github.workspace }}/libs/ssl/
      
      - uses: actions/download-artifact@v8
        if: steps.cache-libs.outputs.cache-hit != 'true'
        with:
          name: h264-${{ env.OPENH264_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
          path: ${{ github.workspace }}/libs/h264/
      
      - uses: actions/download-artifact@v8
        if: steps.cache-libs.outputs.cache-hit != 'true'
        with:
          name: opus-${{ matrix.arch }}
          path: ${{ github.workspace }}/libs/opus/
      
      - uses: actions/download-artifact@v8
        if: steps.cache-libs.outputs.cache-hit != 'true'
        with:
          name: bcg729-${{ env.BCG729_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
          path: ${{ github.workspace }}/libs/bcg729/

      - name: Validate Deps
        if: steps.cache-libs.outputs.cache-hit != 'true'
        working-directory: libs
        shell: pwsh
        run: ls -R .
          
      - name: Install dependencies
        if: steps.cache-libs.outputs.cache-hit != 'true'
        run: |
          brew install automake autoconf libtool pkg-config swig llvm

      - name: Setup Xcode 26.3
        if: steps.cache-libs.outputs.cache-hit != 'true'
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '26.3'
 
      - name: Build pjsip
        id: build-pj-ios
        if: steps.cache-libs.outputs.cache-hit != 'true'
        run: |
          cp -f ${{ github.workspace }}/res/pj/config_site.h ${{ env.PJSIP_PATH }}/pjlib/include/pj/config_site.h
          mkdir -p ~/pjsip_build/bundle/${{ matrix.arch }}
          cp -r ${{ env.PJSIP_PATH }} ~/pjsip_build/bundle/${{ matrix.arch }}
          cd ~/pjsip_build/bundle/${{ matrix.arch }}/pjsip
          export SDKVERSION=$(xcrun -sdk ${{ matrix.sdk }} --show-sdk-version)
          export IPHONESDK="${{ matrix.platform}}${SDKVERSION}.sdk" 
          export DEVPATH="$(xcode-select -p)/Platforms/${{ matrix.platform }}.platform/Developer"
          export SDKROOT="${DEVPATH}/SDKs/${{ matrix.platform }}.sdk"
          export SDKPATH="${DEVPATH}/SDKs/${IPHONESDK}" 

          if [ "${{ matrix.arch }}" == "arm64" ]; then
            export CFLAGS=" -arch ${{ matrix.resolved_arch }} ${CFLAGS}"
            export LDFLAGS="-arch ${{ matrix.resolved_arch }} ${LDFLAGS}"
            export MIN_IOS=-miphoneos-version-min=${{ env.MIN_IOS_VERSION }}
          else
            export CFLAGS=" -arch ${{ matrix.resolved_arch }} ${CFLAGS} -O2 -m64"
            export LDFLAGS="-arch ${{ matrix.resolved_arch }} ${LDFLAGS} -O2 -m64"
            export MIN_IOS="-mios-simulator-version-min=${{ env.MIN_IOS_VERSION }}"
          fi
           
          ARCH="-arch ${{ matrix.resolved_arch }}" ./configure-iphone \
              --with-bcg729=${{ github.workspace }}/libs/bcg729 \
              --with-ssl=${{ github.workspace }}/libs/ssl \
              --with-opus=${{ github.workspace }}/libs/opus \
              --with-openh264=${{ github.workspace }}/libs/h264
          echo "FLAGS AFTER CONFIGURE:"
          echo ""
          echo "CFLAGS=${CFLAGS}"
          echo "LDFLAGS=${LDFLAGS}"
          # Deduplicar CFLAGS
          export CFLAGS=$(echo "$CFLAGS" | tr ' ' '\n' | awk '!seen[$0]++' | tr '\n' ' ')
          # Deduplicar LDFLAGS
          export LDFLAGS=$(echo "$LDFLAGS" | tr ' ' '\n' | awk '!seen[$0]++' | tr '\n' ' ')
          echo "FLAGS AFTER DEDUPLICATE:"
          echo ""
          echo "CFLAGS=${CFLAGS}"
          echo "LDFLAGS=${LDFLAGS}"
          
          make dep
          make

          cd ./pjsip-apps/src/swig/csharp/pjsua2maui
          cp -v ${{ github.workspace }}/res/pj/pjsua2maui/Makefile .

          echo "Starting make iOS"
          make
          echo "Starting copy"
          
          mkdir -p ${{ github.workspace }}/merge
          mkdir -p ${{ github.workspace }}/pjsip-${{matrix.arch}}/lib
          mkdir -p ${{ github.workspace }}/pjsip-${{matrix.arch}}/cs
          cp -v -f ${{ github.workspace }}/libs/ssl/lib/*.a ${{ github.workspace }}/merge
          cp -v -f ${{ github.workspace }}/libs/opus/lib/*.a ${{ github.workspace }}/merge
          cp -v -f ${{ github.workspace }}/libs/bcg729/lib/*.a ${{ github.workspace }}/merge
          cp -v -f ${{ github.workspace }}/libs/h264/lib/*.a ${{ github.workspace }}/merge
          cp -v -f ./pjsua2maui/Platforms/iOS/lib/${{ matrix.resolved_arch }}/*.a ${{ github.workspace }}/merge
          libtool -static -o ${{ github.workspace }}/pjsip-${{matrix.arch}}/lib/libpjsua2.a ${{ github.workspace }}/merge/*.a
          cp -v -f ./pjsua2maui/Platforms/iOS/pjsua2/*.cs ${{ github.workspace }}/pjsip-${{matrix.arch}}/cs
          
        env:
          BUILD_THREADS: $(sysctl -n hw.ncpu)

      - name: Upload artifacts
        uses: actions/upload-artifact@v7
        with:
          name: pjsip-${{ env.PJSIP_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
          path: ${{ github.workspace }}/pjsip-${{matrix.arch}}
          overwrite: true
       
  bcg729-mac:
    runs-on: macos-26
    name: bcg729-mac
    strategy:
      fail-fast: false
      matrix:
        arch: [arm64, x86_64]
        include:
          - platform: MacOSX
          - lib_name: bcg729
          - sdk: macosx
    steps:  
      - name: Cache BCG729 Built SDK
        id: cache-libs
        uses: actions/cache@v5
        with:
          path: ${{ github.workspace }}/${{ matrix.lib_name }}-${{ env.BCG729_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
          key: ${{ matrix.lib_name }}-${{ env.BCG729_VERSION }}-${{ matrix.platform }}-${{ env.SDKVERSION }}-${{ matrix.arch }}

      - name: Clone BCG729
        if: steps.cache-libs.outputs.cache-hit != 'true'
        run: |
          git clone --branch $BCG729_VERSION https://github.com/BelledonneCommunications/bcg729.git 

      - name: Setup Xcode 26.3
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '26.3'
  
      - name: Install deps
        if: steps.cache-libs.outputs.cache-hit != 'true'
        run: |
          brew install autoconf automake cmake libtool llvm nasm 
        shell: sh

      - name: Build BCG729 for MacCatalyst
        if: steps.cache-libs.outputs.cache-hit != 'true'
        working-directory: bcg729
        shell: sh
        run: |
          echo "-- Building BCG729 for iOS"
          echo "-- Building arch ${{ matrix.arch }}"
          xcode_dev_path=$(xcode-select -print-path)
          os_platform="MacOSX"
          sdk_path="${xcode_dev_path}/Platforms/${os_platform}.platform/Developer/SDKs/${os_platform}.sdk"
          mkdir -p ../logs
          mkdir -p ../../install

          cmake .                                                               \
            -DCMAKE_POLICY_VERSION_MINIMUM=3.5                                  \
            -DCMAKE_BUILD_TYPE=Release                                          \
            -DCMAKE_SYSTEM_NAME=Darwin                                          \
            -DCMAKE_C_COMPILER=$(which clang)                                   \
            -DCMAKE_MAKE_PROGRAM:PATH=$(which make)                             \
            -DCMAKE_SKIP_INSTALL_RPATH=ON                                       \
            -DCMAKE_C_FLAGS="-target ${{ matrix.arch }}-apple-ios15.0-macabi"   \
            -DBUILD_SHARED_LIBS=OFF                                             \
            -DENABLE_SHARED=OFF                                                 \
            -DENABLE_STATIC=ON                                                  \
            -DCMAKE_INSTALL_PREFIX=${{ env.BUILT_SDK_PATH }}                    \
            -DCMAKE_OSX_SYSROOT="$sdk_path"                                     \
            -DCMAKE_OSX_ARCHITECTURES=${{ matrix.arch }}                        \
            > ../logs/${{ matrix.lib_name }}-config-${{ matrix.arch }}.log 

          make -j$(sysctl -n hw.logicalcpu) \
            > ../logs/${{ matrix.lib_name }}-build-${{ matrix.arch }}.log 

          make install \
            > ../logs/${{ matrix.lib_name }}-install-${{ matrix.arch }}.log
        env: 
          BUILT_SDK_PATH:  ${{ github.workspace }}/${{ matrix.lib_name }}-${{ env.BCG729_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk

      - uses: actions/upload-artifact@v7
        name: Upload error logs
        if: failure()
        with:
          name: ${{ matrix.lib_name }}-${{ env.BCG729_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk-logs
          path: |
            logs/*.log        

      - uses: actions/upload-artifact@v7
        name: Upload BCG ${{ matrix.arch }} artifact 
        with:
          name: ${{ matrix.lib_name }}-${{ env.BCG729_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
          path: |
            ${{ env.BUILT_SDK_PATH }}/**/*
            logs/*.log
        env:
          BUILT_SDK_PATH: ${{ github.workspace }}/${{ matrix.lib_name }}-${{ env.BCG729_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
 
  openh264-mac:
    runs-on: macos-26
    name: openh264-mac
    strategy:
      fail-fast: false
      matrix:
        arch: [arm64, x86_64]
        include:
          - platform: MacOSX
          - lib_name: h264
          - sdk: macosx
    steps:
      - name: Cache H264 Built SDK
        id: cache-libs
        uses: actions/cache@v5
        with:
          path: ${{ github.workspace }}/${{ matrix.lib_name }}-${{ env.OPENH264_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
          key: ${{ matrix.lib_name }}-${{ env.OPENH264_VERSION }}-${{ matrix.platform }}-${{ env.SDKVERSION }}-${{ matrix.arch }}
      
      - name: Clone OpenH264
        if: steps.cache-libs.outputs.cache-hit != 'true'
        run: |
          git clone --branch v2.3.0 https://github.com/cisco/openh264.git

      - name: Setup Xcode 26.3
        uses: maxim-lobanov/setup-xcode@v1
        if: steps.cache-libs.outputs.cache-hit != 'true'
        with:
          xcode-version: '26.3'
  
      - name: Install deps
        if: steps.cache-libs.outputs.cache-hit != 'true'
        run: |
          brew install autoconf automake cmake libtool llvm nasm 
        shell: sh

      - name: Build OpenH264
        if: steps.cache-libs.outputs.cache-hit != 'true'
        id: build
        working-directory: openh264
        run: |
          mkdir -p ../logs
          target_triple="${{ matrix.arch }}-apple-ios${{ env.MIN_MACOSX_VERSION }}-macabi"
          version_min="-target ${target_triple}"        
          
          export OS="darwin"
          sdk_path=$(xcrun --sdk ${{ matrix.sdk }} --show-sdk-path)
          developer_path=$(xcode-select -p)

          h264_makefile="Makefile"
          h264_makefile_bak="Makefile.bak" 
          h264_prefix="${{ env.BUILT_SDK_PATH }}"
          
          if [ "${{ matrix.arch }}" == "arm64" ]; then
            export ARCH="arm64"
            # SOCI=No disables production of dylib
            EXTRA_MAKE_ARGS="USE_ASM=Yes SOCI=No"
          else
            export ARCH="x86_64"
            # USE_ASM=No disables files include and search from x86/ folder
            # SOCI=No disables production of dylib
            EXTRA_MAKE_ARGS="USE_ASM=No SOCI=No"
          fi
          
          echo "Building OpenH264 for ${{ matrix.arch }} (${{ matrix.platform }}, OS=$OS, SDK=${{ matrix.sdk }}, Target=$target_triple)"

          cp "${h264_makefile}" "${h264_makefile_bak}"
          sed -i.deleteme "s|^PREFIX=.*|PREFIX=${h264_prefix}|" "${h264_makefile}"
          rm -f ${h264_makefile}.deleteme
          
          export CFLAGS="-arch ${{ matrix.arch }} ${version_min} -isysroot ${sdk_path}"
          export LDFLAGS="-arch ${{ matrix.arch }} ${version_min} -isysroot ${sdk_path}"
          export SYSROOT="${sdk_path}"

          # Compilação aplicando as flags mapeadas
          make ARCH=$ARCH $EXTRA_MAKE_ARGS SDK_MIN=${{ env.MIN_MACOSX_VERSION }} v=No libopenh264.a > ../logs/${{ matrix.lib_name }}-config-${{ matrix.arch }}.log 2>&1 || exit 1

          make ARCH=$ARCH $EXTRA_MAKE_ARGS SDK_MIN=${{ env.MIN_MACOSX_VERSION }} v=No install-static install-headers > ../logs/${{ matrix.lib_name }}-install-${{ matrix.arch }}.log 2>&1 || exit 1

          make ARCH=$ARCH $EXTRA_MAKE_ARGS SDK_MIN=${{ env.MIN_MACOSX_VERSION }} v=No clean > ../logs/${{ matrix.lib_name }}-clean-${{ matrix.arch }}.log 2>&1 || exit 1

          mv "${h264_makefile_bak}" "${h264_makefile}"

        shell: bash
        env:
          BUILT_SDK_PATH: ${{ github.workspace }}/${{ matrix.lib_name }}-${{ env.OPENH264_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk

      - uses: actions/upload-artifact@v7
        name: Upload error logs
        if: failure()
        with:
          name: ${{ matrix.lib_name }}-${{ env.SDK_SUFFIX }}-logs
          path: |
            logs/*.log
        env:
          SDK_SUFFIX: ${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
            
      - uses: actions/upload-artifact@v7
        with:
          name: h264-${{ env.OPENH264_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
          path: |
            ${{ env.BUILT_SDK_PATH }}/**/*
            logs/*.log
        env: 
          BUILT_SDK_PATH: ${{ github.workspace }}/${{ matrix.lib_name }}-${{ env.OPENH264_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
 
  openssl-mac:
    runs-on: macos-26
    name: openssl-mac
    strategy:
      fail-fast: false
      matrix:
        arch: [arm64, x86_64]
        include:
          - platform: MacOSX
          - lib_name: ssl
          - sdk: macosx
    steps:
      
      - name: Cache SSL Built SDK
        id: cache-libs
        uses: actions/cache@v5
        with:
          path: ${{ github.workspace }}/${{ matrix.lib_name }}-${{ env.OPENSSL_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
          key: ${{ matrix.lib_name }}-${{ env.OPENSSL_VERSION }}-${{ matrix.platform }}-${{ env.SDKVERSION }}-${{ matrix.arch }}

      - name: Clone OpenSSL
        if: steps.cache-libs.outputs.cache-hit != 'true'
        run: |
          git clone --branch $OPENSSL_VERSION https://github.com/openssl/openssl.git --recursive

      - name: Setup Xcode 26.3
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '26.3'
  
      - name: Install deps
        if: steps.cache-libs.outputs.cache-hit != 'true'
        run: |
          brew install nasm autoconf automake libtool 
        shell: sh
          
      - name: Build OpenSSL
        if: steps.cache-libs.outputs.cache-hit != 'true'
        working-directory: openssl
        shell: bash 
        run: |
          mkdir -p ../logs
          TARGET="darwin64-${{ matrix.arch }}-cc"
          HOST_TARGET="${{ matrix.arch }}-apple-ios15.0-macabi"
          
          export CC="clang -target $HOST_TARGET"
          export CFLAGS="-fPIC"

 
          INCLUDE_DIR=""
          OPENSSLCONF_ALL=()
          LIBSSL_IOS=()
          LIBSSL_IOSSIM=()
          LIBCRYPTO_IOS=()
          LIBCRYPTO_IOSSIM=()
          export OPENSSL_LOCAL_CONFIG_DIR=${{ github.workspace }}/res/ssl/config
          export SDKVERSION=${{ env.SDKVERSION }}
          export MACOSX_MIN_SDK_VERSION=15.0
          export CONFIG_DISABLE_BITCODE=true 
          export CONFIG_OPTIONS="no-dso no-shared no-engine no-tests no-unit-test no-ssl3 no-comp enable-ec_nistp_64_gcc_128"
                    
          chmod u+x ./Configure
          
          PLATFORM=MacOSX
          ARCH=${{ matrix.arch }}
          # Cross compile references, see Configurations/10-main.conf
          export CROSS_COMPILE="${{ env.DEVELOPER }}/Toolchains/XcodeDefault.xctoolchain/usr/bin/"
          export CROSS_TOP="${{ env.DEVELOPER }}/Platforms/MacOSX.platform/Developer"
          export CROSS_SDK="MacOSX${SDKVERSION}.sdk"

          # Prepare TARGET_DIR  
          mkdir -p "${{ env.BUILT_SDK_PATH }}"
          ## Determine config options
          # Add build target, --prefix and prevent async (references to getcontext(),
          # setcontext() and makecontext() result in App Store rejections) and creation
          # of shared libraries (default since 1.1.0)
          LOCAL_CONFIG_OPTIONS="${TARGET} --prefix=${{ env.BUILT_SDK_PATH }} ${CONFIG_OPTIONS} no-async no-shared"

          # Only relevant for 64 bit builds
          if [[ "${CONFIG_ENABLE_EC_NISTP_64_GCC_128}" == "true" ]]; then
              LOCAL_CONFIG_OPTIONS="${LOCAL_CONFIG_OPTIONS} enable-ec_nistp_64_gcc_128"
          fi

          export CFLAGS="-isysroot ${CROSS_TOP}/SDKs/${CROSS_SDK} -mios-version-min=${MACOSX_MIN_SDK_VERSION}"
          export LDFLAGS="-isysroot ${CROSS_TOP}/SDKs/${CROSS_SDK}"

          # Run Configure
          ./Configure ${LOCAL_CONFIG_OPTIONS} no-tests > ../logs/openssl-config-${{ matrix.arch }}.log 

          # Run make
          echo "  Make (using ${{ env.BUILD_THREADS }} thread(s))..." 
          make build_libs -j "${{ env.BUILD_THREADS }}"  > ../logs/openssl-build-${{ matrix.arch }}.log
  
          # Run make install
          set -e 
          make install_dev >> ../logs/openssl-install-${{ matrix.arch }}.log  
  
        env:
          DEVELOPER: $(xcode-select -print-path)
          BUILD_THREADS: $(sysctl -n hw.logicalcpu) 
          BUILT_SDK_PATH: ${{ github.workspace }}/${{ matrix.lib_name }}-${{ env.OPENSSL_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk

      - uses: actions/upload-artifact@v7
        name: Upload error logs
        if: failure()
        with:
          name: ${{ matrix.lib_name }}-${{ env.SDK_SUFFIX }}-logs
          path: |
            logs/*.log
        env:
          SDK_SUFFIX: ${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk

      - name: Upload OpenSSL Artifacts
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.lib_name }}-${{ env.OPENSSL_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
          path: |
            ${{ env.BUILT_SDK_PATH }}
            logs/*.log
        env:
          BUILT_SDK_PATH: ${{ github.workspace }}/${{ matrix.lib_name }}-${{ env.OPENSSL_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
      
  opus-mac:
    runs-on: macos-26
    name: opus-mac
    strategy:
      fail-fast: false
      matrix:
        arch: [arm64, x86_64]
        include:
          - platform: MacOSX
          - lib_name: opus   
          - sdk: macosx 
    steps:
      
      - name: Cache opus Built SDK
        id: cache-libs
        uses: actions/cache@v5
        with:
          path: ${{ github.workspace }}/${{ matrix.lib_name }}-${{ env.OPUS_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
          key: ${{ matrix.lib_name }}-${{ env.OPUS_VERSION }}-${{ matrix.platform }}-${{ env.SDKVERSION }}-${{ matrix.arch }}

      - name: Clone Opus
        if: steps.cache-libs.outputs.cache-hit != 'true'
        run: |
          
          git clone --branch v1.4 https://github.com/xiph/opus.git

      - name: Setup Xcode 26.3
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '26.3'
  
      - name: Install deps
        if: steps.cache-libs.outputs.cache-hit != 'true'
        run: |
          brew install nasm autoconf automake libtool 
        shell: sh
          
      - name: Build Opus
        id: build  # Adicionado o ID 'build' para que o '${{ steps.build.outputs.artifact }}' funcione no upload
        working-directory: opus
        if: steps.cache-libs.outputs.cache-hit != 'true'
        run: |
          mkdir -p ../logs

          opus_version="1.4"
          opus_opt_cflags="-O3 -ffast-math -flto -g"
          opus_opt_ldflags="-flto"
          DEVELOPER=$(xcode-select -print-path)
          SDKVERSION=$(xcrun -sdk ${{ matrix.sdk }} --show-sdk-version)
          PLATFORM="MacOSX"
          
          mkdir -p "${{ env.BUILT_SDK_PATH }}/include"
          mkdir -p "${{ env.BUILT_SDK_PATH }}/lib"

          echo "-- Building Opus for ${{ matrix.arch }} on ${PLATFORM} (Mac Catalyst)"

          if [ "${{ matrix.arch }}" == "arm64" ]; then
            HOST="arm-apple-darwin"
            TARGET="arm64-apple-ios15.0-macabi"
            opus_opt_cflags="-O3 -ffast-math -flto -g -Wno-cast-align"
          else
            HOST="x86_64-apple-darwin"
            TARGET="x86_64-apple-ios15.0-macabi"
            opus_opt_cflags="-O3 -ffast-math -flto -g"
          fi

          ./autogen.sh

          ISYSROOT="${DEVELOPER}/Platforms/MacOSX.platform/Developer/SDKs/MacOSX${SDKVERSION}.sdk"

          CC="clang"                                                                                                          \
          ./configure                                                                                                         \
            --host=$HOST                                                                                                      \
            --disable-shared                                                                                                  \
            --with-pic                                                                                                        \
            --disable-extra-programs                                                                                          \
            --disable-doc                                                                                                     \
            --enable-static                                                                                                   \
            --enable-float-approx                                                                                             \
            --prefix="${{ env.BUILT_SDK_PATH }}"                                                                                           \
            LDFLAGS="${opus_opt_ldflags} -target $TARGET -isysroot ${ISYSROOT} -L${{ env.BUILT_SDK_PATH }}/lib"                            \
            CFLAGS="${opus_opt_cflags} -target $TARGET -arch ${{ matrix.arch }} -isysroot ${ISYSROOT} -I${{ env.BUILT_SDK_PATH }}/include" \
            > ../logs/opus-config-${{ matrix.arch }}.log 

          make -j$(sysctl -n hw.logicalcpu) \
            > ../logs/opus-build-${{ matrix.arch }}.log 

          make install \
            >> ../logs/opus-build-${{ matrix.arch }}.log 
        env:
          BUILT_SDK_PATH: ${{ github.workspace }}/${{ matrix.lib_name }}-${{ env.OPUS_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk

      - uses: actions/upload-artifact@v7
        name: Upload error logs
        if: failure()
        with:
          name: ${{ matrix.lib_name }}-${{ env.OPUS_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}-logs
          path: |
            logs/*.log 
          
      - uses: actions/upload-artifact@v7
        name: Upload opus artifact
        with:
          name: ${{ matrix.lib_name }}-${{ env.OPUS_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
          path: |
            ${{ env.BUILT_SDK_PATH }}
            logs/*.log
        env:
          BUILT_SDK_PATH: ${{ github.workspace }}/${{ matrix.lib_name }}-${{ env.OPUS_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
      
  pjsip-mac:
    name: pjsip - mac 
    runs-on: macos-26
    needs: [bcg729-mac, openh264-mac, openssl-mac, opus-mac]
    strategy:
      fail-fast: false
      matrix:
        arch: [arm64, x86_64]
        include:
        - lib_name: pjsip
        - platform: MacOSX
        - sdk: macosx
    steps:
      - uses: actions/checkout@v6
      
      - name: Cache pjsip Built SDK
        uses: actions/cache@v5
        id: cache-libs
        with:
          path: ${{ github.workspace }}/${{ matrix.lib_name}}-${{ env.PJSIP_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
          key: ${{ matrix.lib_name}}-${{ env.PJSIP_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}

      - name: Clone PJSIP
        if: steps.cache-libs.outputs.cache-hit != 'true'
        run: |
          git clone --branch $PJSIP_VERSION https://github.com/pjsip/pjproject.git

      - name: Setup Xcode 26.3
        uses: maxim-lobanov/setup-xcode@v1
        if: steps.cache-libs.outputs.cache-hit != 'true'
        with:
          xcode-version: '26.3'
  
      - name: Install deps
        if: steps.cache-libs.outputs.cache-hit != 'true'
        run: |
          brew install nasm autoconf automake libtool swig pkg-config llvm
        shell: sh

      - uses: actions/download-artifact@v8
        if: steps.cache-libs.outputs.cache-hit != 'true'
        name: Download bcg729 macos
        with:
          name: ${{ env.BCG_SDK_NAME }}
          path: ${{ github.workspace }}/libs
        env:
          BCG_SDK_NAME: bcg729-${{ env.BCG729_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
     
      - uses: actions/download-artifact@v8
        if: steps.cache-libs.outputs.cache-hit != 'true'
        name: Download H264 macos
        with:
          name: ${{ env.H264_SDK_NAME }}
          path: ${{ github.workspace }}/libs
        env:
          H264_SDK_NAME: h264-${{ env.OPENH264_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
       
      - uses: actions/download-artifact@v8
        if: steps.cache-libs.outputs.cache-hit != 'true'
        name: Download SSL macos
        with:
          name: ${{ env.SSL_SDK_NAME }}
          path: ${{ github.workspace }}/libs
        env:
          SSL_SDK_NAME: ssl-${{ env.OPENSSL_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
      
      - uses: actions/download-artifact@v8
        if: steps.cache-libs.outputs.cache-hit != 'true'
        name: Download opus macos (${{ matrix.arch }})
        with:
          name: ${{ env.OPUS_SDK_NAME }}
          path: ${{ github.workspace }}/libs
        env:
          OPUS_SDK_NAME: opus-${{ env.OPUS_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
          
      - name: Copy config_site.h to PJSIP
        if: steps.cache-libs.outputs.cache-hit != 'true'
        run: |
          TARGET_CONFIG_DIR="pjproject/pjlib/include/pj"
          mkdir -p "$TARGET_CONFIG_DIR"
          cp "${{ github.workspace }}/res/pj/pjsua2maui/config_site.h" "$TARGET_CONFIG_DIR/config_site.h"
    
      - name: Inspect Dependencies Structure
        if: steps.cache-libs.outputs.cache-hit != 'true'
        run: |
          echo "=== Library folders structure check ==="
          ls  ${{ github.workspace }}/libs/${{ env.BCG_SDK_NAME }} && echo "bcg729 found"
          ls  ${{ github.workspace }}/libs/${{ env.H264_SDK_NAME }} && echo "h264 found"
          ls  ${{ github.workspace }}/libs/${{ env.OPUS_SDK_NAME }} && echo "opus  found"
          ls  ${{ github.workspace }}/libs/${{ env.SSL_SDK_NAME }} && echo "ssl  found"
          ls -R ${{ github.workspace }}/libs
        env:
          BCG_SDK_NAME: bcg729-${{ env.BCG729_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
          H264_SDK_NAME: h264-${{ env.OPENH264_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
          OPUS_SDK_NAME: opus-${{ env.OPUS_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
          SSL_SDK_NAME: ssl-${{ env.OPENSSL_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
      - name: Build 
        id: build-pjsip-mac
        if: steps.cache-libs.outputs.cache-hit != 'true'
        working-directory: pjproject
        run: |
          mkdir -p ../logs
           
          CATALYST_SYSTEM_INCLUDES="$SDKPATH/System/iOSSupport/usr/include"
          export ARCH=${{ matrix.arch }}
          export TARGET="${{ matrix.arch }}-apple-ios15.0-macabi"
          export CC="clang -target $TARGET"
          export CXX="clang++ -target $TARGET"
          export SDKVERSION=${{ env.SDKVERSION }}
          export MACOSXSDK="${{ matrix.platform}}${SDKVERSION}.sdk" 
          export SDKPATH=$(xcrun -sdk ${{ matrix.sdk }} --show-sdk-path)
          CATALYST_SYSTEM_FRAMEWORKS="$SDKPATH/System/iOSSupport/System/Library/Frameworks"
          CATALYST_SYSTEM_INCLUDES="$SDKPATH/System/iOSSupport/usr/include"
          export CFLAGS="${CFLAGS} -I${{ env.BCG_ROOT }}/include -I${{ env.H264_ROOT }}/include -I${{ env.SSL_ROOT }}/include -I${{ env.OPUS_ROOT }}/include "
          export LDFLAGS="${LDFLAGS} -L${{ env.BCG_ROOT }}/lib -lbcg729 -L${{ env.H264_ROOT }}/lib -lopenh264 -L${{ env.SSL_ROOT }}/lib -lssl -lcrypto -L${{ env.OPUS_ROOT }}/lib -lopus"
          if [[ '${{ matrix.arch }}' == 'arm64' ]]; then
            export CFLAGS="-arch arm64 -target arm64-apple-ios15.0-macabi -isysroot $SDKPATH -fPIC ${CFLAGS} -isystem $CATALYST_SYSTEM_INCLUDES -iframework $CATALYST_SYSTEM_FRAMEWORKS" 
            export CXXFLAGS="-arch arm64 -target arm64-apple-ios15.0-macabi -isysroot $SDKPATH -fPIC ${CXXFLAGS} -isystem $CATALYST_SYSTEM_INCLUDES -iframework $CATALYST_SYSTEM_FRAMEWORKS"
            export LDFLAGS="-arch arm64 -target arm64-apple-ios15.0-macabi -isysroot $SDKPATH ${LDFLAGS} -F$CATALYST_SYSTEM_FRAMEWORKS"
          else
            export CFLAGS="-arch x86_64 -target x86_64-apple-ios15.0-macabi -isysroot $SDKPATH -fPIC ${CFLAGS} -isystem $CATALYST_SYSTEM_INCLUDES -iframework $CATALYST_SYSTEM_FRAMEWORKS"
            export CXXFLAGS="-arch x86_64 -target x86_64-apple-ios15.0-macabi -isysroot $SDKPATH -fPIC ${CXXFLAGS} -isystem $CATALYST_SYSTEM_INCLUDES -iframework $CATALYST_SYSTEM_FRAMEWORKS"
            export LDFLAGS="-arch x86_64 -target x86_64-apple-ios15.0-macabi -isysroot $SDKPATH ${LDFLAGS} -F$CATALYST_SYSTEM_FRAMEWORKS"
          fi

          ./configure                            \
            --host=${{ matrix.arch }}-apple-darwin            \
            --disable-shared                     \
            --with-bcg729=${{ env.BCG_ROOT }}    \
            --with-ssl=${{ env.SSL_ROOT }}       \
            --with-opus=${{ env.OPUS_ROOT }}     \
            --with-openh264=${{ env.H264_ROOT }} \
            CFLAGS="$CFLAGS"                     \
            LDFLAGS="$LDFLAGS"                   \
            CXXFLAGS="$CXXFLAGS" > ../logs/pjsip-config-${{ matrix.arch }}.log || exit 1

          echo "FLAGS AFTER CONFIGURE:"
          echo "CFLAGS=${CFLAGS}"
          echo "LDFLAGS=${LDFLAGS}"
          
          export CFLAGS=$(echo "$CFLAGS" | tr ' ' '\n' | awk '!seen[$0]++' | tr '\n' ' ')
          export LDFLAGS=$(echo "$LDFLAGS" | tr ' ' '\n' | awk '!seen[$0]++' | tr '\n' ' ')

          echo "starting make dep"

          make dep > ../logs/pjsip-dep-${{ matrix.arch }}.log || exit 1 

          echo 'Done make dep' >> ../logs/pjsip-dep-${{ matrix.arch }}.log

          echo "starting make"

          make lib > ../logs/pjsip-build-${{ matrix.arch }}.log || exit 1

          echo 'Done make lib' >> ../logs/pjsip-build-${{ matrix.arch }}.log

          cd ./pjsip-apps/src/swig/csharp/pjsua2maui/
          
          cp -v -f ${{ github.workspace}}/res/pj/pjsua2maui/Makefile . 

          echo "starting make mac"

          make -j$(sysctl -n hw.logicalcpu) > ${{ github.workspace }}/logs/pjsip-swig-${{ matrix.arch }}.log || exit 1
          
          echo 'Done make mac' >> ${{ github.workspace }}/logs/pjsip-swig-${{ matrix.arch }}.log
           
          mkdir -p "${{ env.BUILT_SDK_PATH }}/lib"
          mkdir -p "${{ env.BUILT_SDK_PATH }}/cs" 

          cp -v -f ${{ env.BCG_ROOT }}/lib/*.a . 
          cp -v -f ${{ env.H264_ROOT }}/lib/*.a .
          cp -v -f ${{ env.OPUS_ROOT }}/lib/*.a .
          cp -v -f ${{ env.SSL_ROOT }}/lib/*.a .
          cp -v -f ./pjsua2maui/Platforms/MacCatalyst/lib/${{ matrix.arch }}/libpjsua2.a .
          
          libtool -static -o "${{ env.BUILT_SDK_PATH }}/lib/libpjsua2.a" ./*.a
          
          cp -v -f ./pjsua2maui/Platforms/MacCatalyst/pjsua2/*.cs "${{ env.BUILT_SDK_PATH }}/cs/"

        shell: bash
        env:
          BCG_SDK_NAME: bcg729-${{ env.BCG729_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
          H264_SDK_NAME: h264-${{ env.OPENH264_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
          OPUS_SDK_NAME: opus-${{ env.OPUS_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
          SSL_SDK_NAME: ssl-${{ env.OPENSSL_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
          BCG_ROOT: ${{ github.workspace }}/libs/bcg729-${{ env.BCG729_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
          H264_ROOT: ${{ github.workspace }}/libs/h264-${{ env.OPENH264_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
          OPUS_ROOT: ${{ github.workspace }}/libs/opus-${{ env.OPUS_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
          SSL_ROOT: ${{ github.workspace }}/libs/ssl-${{ env.OPENSSL_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
          BUILT_SDK_PATH:  ${{ github.workspace }}/${{ matrix.lib_name}}-${{ env.PJSIP_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk

      - uses: actions/upload-artifact@v7
        name: Upload error logs
        if: steps.cache-libs.outputs.cache-hit != 'true' && failure()  
        with:
          name: ${{ matrix.lib_name }}-${{ env.SDK_SUFFIX }}-logs
          path: | 
            logs/*.log
            pjproject/*.log
        env:
          SDK_SUFFIX: ${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk

      - uses: actions/upload-artifact@v7
        name: Upload pjsip artifact
        with:
          name: ${{ matrix.lib_name}}-${{ env.PJSIP_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
          path: |
            ${{ env.BUILT_SDK_PATH }}
        env:
          BUILT_SDK_PATH: ${{ github.workspace }}/${{ matrix.lib_name}}-${{ env.PJSIP_VERSION }}-${{ matrix.platform }}${{ env.SDKVERSION }}-${{ matrix.arch }}.sdk
   
  bcg729-win:
    name: bcg729-win (${{ matrix.arch }})
    runs-on: windows-2022
    strategy:
      fail-fast: false
      matrix:
        arch: [x64, arm64]

    steps:
      - name: Checkout
        uses: actions/checkout@v4
  
      - name: Cache BCG729 Built SDK
        id: cache-libs
        uses: actions/cache@v5
        with:
          path: ${{ github.workspace }}/deps/bcg729
          key: bcg729-${{ env.BCG729_VERSION }}-win-${{ matrix.arch }}
    
      - name: Setup MSVC
        if: steps.cache-libs.outputs.cache-hit != 'true'
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: ${{ matrix.arch }}

      - name: Setup CMake
        uses: lukka/get-cmake@latest
        if: steps.cache-libs.outputs.cache-hit != 'true'

      - name: Verify Compiler
        if: steps.cache-libs.outputs.cache-hit != 'true'
        shell: cmd
        run: |
            echo Visual Studio environment loaded for ${{ matrix.arch }}
            where cl
            where msbuild

      - name: Clone BCG729
        if: steps.cache-libs.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
            git clone --branch "${env:BCG729_VERSION}" --depth 1 https://github.com/BelledonneCommunications/bcg729.git bcg729
            
      - name: Remove Legacy MSVC Headers (ARM64 Only)
        if: matrix.arch == 'ARM64' && steps.cache-libs.outputs.cache-hit != 'true'
        shell: pwsh
        working-directory: bcg729
        run: |
          if (Test-Path "include/MSVC") {
            Remove-Item -Path "include/MSVC" -Recurse -Force
            Write-Host "Removed old MSVC implementation in order to use the new available from Win SDK 10.0.26100.0" -ForegroundColor Green
          }

      - name: Build bcg729
        if: steps.cache-libs.outputs.cache-hit != 'true'
        shell: cmd
        working-directory: bcg729
        run: |
          cmake -S . ^
                -B build ^
                -G "Visual Studio 17 2022" ^
                -A ${{ matrix.arch }} ^
                -DBUILD_SHARED_LIBS=OFF ^
                -DCMAKE_SYSTEM_VERSION="10.0.26100.0" ^
                -DCMAKE_POLICY_VERSION_MINIMUM='3.5' ^
                -DCMAKE_INSTALL_PREFIX=%CD%\..\deps\bcg729\

          cmake --build build --config Release --target INSTALL          

      - name: Normalize artifacts
        if: steps.cache-libs.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
            $target = "${env:INSTALL_DIR}\bcg729\${{ matrix.arch }}"

            New-Item -ItemType Directory -Force -Path "$target\lib" | Out-Null
            New-Item -ItemType Directory -Force -Path "$target\include\bcg729" | Out-Null

            Get-ChildItem bcg729/build -Recurse -Filter *.lib |
            Copy-Item -Destination "$target\lib" -Force

            Get-ChildItem bcg729/include -Recurse -Filter *.h |
            Copy-Item -Destination "$target\include\bcg729" -Force

      - name: List generated files
        if: steps.cache-libs.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
            Get-ChildItem "${env:INSTALL_DIR}\bcg729" -Recurse
 
      - name: Upload BCG729 artifacts 
        uses: actions/upload-artifact@v4
        with:
          name: bcg729-win-${{ matrix.arch }}
          path: deps/bcg729/**/*
  
  openh264-win:
    name: openh264-win (${{ matrix.arch }})
    runs-on: windows-2022
    strategy:
      fail-fast: false
      matrix:
        arch: [x64, ARM64]

    steps:
      - name: Checkout
        uses: actions/checkout@v6 

      - name: Cache H264 Built SDK
        id: cache-libs
        uses: actions/cache@v5
        with:
          path: install/openh264/
          key: h264-${{ env.OPENH264_VERSION }}-win-${{ matrix.arch }}
    
      - name: Setup MSVC
        if: steps.cache-libs.outputs.cache-hit != 'true'
        uses: ilammy/msvc-dev-cmd@v1
        with:
          # Se for ARM64, usa o cross-compiler (roda em x64, gera ARM64)
          # Se for x64, usa o compilador nativo x64
          arch: ${{ matrix.arch == 'ARM64' && 'x64_arm64' || 'x64' }}

      - name: Setup NASM
        if: steps.cache-libs.outputs.cache-hit != 'true' && matrix.arch == 'x64'
        uses: ilammy/setup-nasm@v1

      - name: Create directories
        if: steps.cache-libs.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path "${env:NUGET_ARTIFACTS}" | Out-Null
          New-Item -ItemType Directory -Force -Path "${env:LOGS_DIR}" | Out-Null
          New-Item -ItemType Directory -Force -Path "${env:INSTALL_DIR}" | Out-Null

      - name: Clone OpenH264
        if: steps.cache-libs.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
          git clone `
            --branch "${env:OPENH264_VERSION}" `
            --depth 1 `
            https://github.com/cisco/openh264.git `
            openh264

      - name: Build OpenH264
        if: steps.cache-libs.outputs.cache-hit != 'true'
        shell: cmd
        working-directory: openh264
        run: |
          :: Define as variáveis corretas com base na arquitetura da matrix
          if "${{ matrix.arch }}"=="ARM64" (
              set "ARCH_FLAG=arm64"
              set "ASM_FLAG=USE_ASM=No"
          ) else (
              set "ARCH_FLAG=x86_64"
              set "ASM_FLAG="
          )

          :: Executa o build usando NMake/Make do ambiente MSVC
          make OS=msvc ARCH=%ARCH_FLAG% BUILDTYPE=Release ENABLE64BIT=Yes DEBUGSYMBOLS=False %ASM_FLAG%

          if %ERRORLEVEL% neq 0 (
              exit /b %ERRORLEVEL%
          )

      - name: Prepare artifact structure
        if: steps.cache-libs.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
          $target = "install/openh264/${{ matrix.arch }}"

          New-Item -ItemType Directory -Force -Path "$target\bin" | Out-Null
          New-Item -ItemType Directory -Force -Path "$target\lib" | Out-Null
          New-Item -ItemType Directory -Force -Path "$target\include\wels" | Out-Null

      - name: Collect static libraries
        if: steps.cache-libs.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
          $target = "install/openh264/${{ matrix.arch }}/lib"

          Get-ChildItem openh264 -Recurse -Filter *.lib |
            Copy-Item -Destination $target -Force

      - name: Collect headers
        if: steps.cache-libs.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
          $target = "install/openh264/${{ matrix.arch }}/include/wels"

          Get-ChildItem openh264/codec/api/wels -Filter *.h |
            Copy-Item -Destination $target -Force

      - name: List generated files
        if: steps.cache-libs.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
          Get-ChildItem "install/openh264" -Recurse

      - name: Upload OpenH264 artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: openh264-win-${{ matrix.arch }}
          path: |
            install/openh264/${{ matrix.arch }}/**/*

  openssl-win:
    name: openssl-win (${{ matrix.arch }})
    runs-on: windows-2022
    strategy:
      fail-fast: false
      matrix:
        arch: [x64, ARM64]

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Cache SSL Built SDK
        id: cache-libs
        uses: actions/cache@v5
        with:
          path: ${{ github.workspace }}/install/openssl/${{ matrix.arch }}
          key: ssl-${{ env.OPENSSL_VERSION }}-win-${{ matrix.arch }}

      - name: Setup MSVC
        if: steps.cache-libs.outputs.cache-hit != 'true'
        uses: ilammy/msvc-dev-cmd@v1
        with:
          # Se for ARM64, usa o cross-compiler (roda em x64, gera ARM64)
          # Se for x64, usa o compilador nativo x64
          arch: ${{ matrix.arch == 'ARM64' && 'x64_arm64' || 'x64' }}

      - name: Setup NASM
        if: steps.cache-libs.outputs.cache-hit != 'true' && matrix.arch == 'x64'
        uses: ilammy/setup-nasm@v1

      - name: Create directories
        if: steps.cache-libs.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path "${env:NUGET_ARTIFACTS}" | Out-Null
          New-Item -ItemType Directory -Force -Path "${env:LOGS_DIR}" | Out-Null
          New-Item -ItemType Directory -Force -Path "${env:INSTALL_DIR}" | Out-Null

      - name: Clone OpenSSL
        if: steps.cache-libs.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
          git clone `
            --branch "${env:OPENSSL_VERSION}" `
            --depth 1 `
            https://github.com/openssl/openssl.git `
            openssl

      - name: Configure OpenSSL
        if: steps.cache-libs.outputs.cache-hit != 'true'
        shell: cmd
        working-directory: openssl
        run: |
          :: Define o target correto do Perl com base na arquitetura da matrix
          if "${{ matrix.arch }}"=="ARM64" (
              set "SSL_TARGET=VC-WIN64-ARM"
          ) else (
              set "SSL_TARGET=VC-WIN64A"
          )

          :: Executa a configuração apontando para o prefixo da arquitetura atual
          perl Configure %SSL_TARGET% no-shared no-tests ^
            --prefix=%INSTALL_DIR%\openssl\${{ matrix.arch }} ^
            --openssldir=%INSTALL_DIR%\openssl\${{ matrix.arch }}

          if %ERRORLEVEL% neq 0 ( 
              exit /b %ERRORLEVEL%
          )

      - name: Build OpenSSL
        if: steps.cache-libs.outputs.cache-hit != 'true'
        shell: cmd
        working-directory: openssl
        run: |
          call nmake  

          if %ERRORLEVEL% neq 0 ( 
              exit /b %ERRORLEVEL%
          )

      - name: Install OpenSSL
        if: steps.cache-libs.outputs.cache-hit != 'true'
        shell: cmd
        working-directory: openssl
        run: |
          call nmake install 

          if %ERRORLEVEL% neq 0 ( 
              exit /b %ERRORLEVEL%
          )
 
      - name: Patch .lib files
        if: steps.cache-libs.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
          $libPath = "${env:INSTALL_DIR}\openssl\${{ matrix.arch }}\lib"
          cp -v "$libPath\libcrypto.lib" "$libPath\crypto.lib"
          cp -v "$libPath\libssl.lib" "$libPath\ssl.lib"
      
      - name: List generated files
        if: steps.cache-libs.outputs.cache-hit != 'true'
        shell: pwsh
        run: | 
          Get-ChildItem "${env:INSTALL_DIR}\openssl" -Recurse

      - name: Upload OpenSSL artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: openssl-win-${{ matrix.arch }}
          path: |
            install/openssl/${{ matrix.arch }}/**/*

  opus-win:
    name: opus-win (${{ matrix.arch }})
    runs-on: windows-2022
    strategy:
      fail-fast: false
      matrix:
        arch: [x64, ARM64]

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Cache opus Built SDK
        id: cache-libs
        uses: actions/cache@v5
        with:
          path: ${{ github.workspace }}/install/opus/${{ matrix.arch }}
          key: opus-${{ env.OPUS_VERSION }}-win-${{ matrix.arch }}

      - name: Setup MSVC
        if: steps.cache-libs.outputs.cache-hit != 'true'
        uses: ilammy/msvc-dev-cmd@v1
        with:
          # Se for ARM64, usa o cross-compiler (roda em x64, gera ARM64)
          # Se for x64, usa o compilador nativo x64
          arch: ${{ matrix.arch == 'ARM64' && 'x64_arm64' || 'x64' }}

      - name: Setup CMake
        if: steps.cache-libs.outputs.cache-hit != 'true'
        uses: lukka/get-cmake@latest

      - name: Create directories
        if: steps.cache-libs.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path "${env:NUGET_ARTIFACTS}" | Out-Null
          New-Item -ItemType Directory -Force -Path "${env:LOGS_DIR}" | Out-Null
          New-Item -ItemType Directory -Force -Path "${env:INSTALL_DIR}" | Out-Null

      - name: Clone Opus
        if: steps.cache-libs.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
          git clone `
            --branch "${env:OPUS_VERSION}" `
            --depth 1 `
            https://github.com/xiph/opus.git `
            opus

      - name: Configure Opus
        if: steps.cache-libs.outputs.cache-hit != 'true'
        shell: cmd
        run: |
          :: O gerador do CMake utiliza o nome exato da arquitetura da matrix (x64 ou ARM64)
          cmake -S opus -B opus\build ^
            -G "Visual Studio 17 2022" ^
            -A ${{ matrix.arch }} ^
            -DCMAKE_BUILD_TYPE=Release ^
            -DCMAKE_INSTALL_PREFIX=%INSTALL_DIR%\opus\${{ matrix.arch }} ^
            -DBUILD_SHARED_LIBS=OFF ^
            -DOPUS_BUILD_SHARED_LIBRARY=OFF ^
            -DOPUS_BUILD_PROGRAMS=OFF ^
            -DOPUS_BUILD_TESTING=OFF ^
            -DCMAKE_POLICY_VERSION_MINIMUM='3.5' ^
            > %LOGS_DIR%\opus-${{ matrix.arch }}-configure.log 2>&1

          if %ERRORLEVEL% neq 0 (
              type %LOGS_DIR%\opus-${{ matrix.arch }}-configure.log
              exit /b %ERRORLEVEL%
          )

      - name: Build Opus
        if: steps.cache-libs.outputs.cache-hit != 'true'
        shell: cmd
        run: |
          cmake --build opus\build ^
            --config Release ^
            --parallel  
          
          if %ERRORLEVEL% neq 0 ( 
              exit /b %ERRORLEVEL%
          )

      - name: Install Opus
        if: steps.cache-libs.outputs.cache-hit != 'true'
        shell: cmd
        run: |
          cmake --install opus\build ^
            --config Release  

          if %ERRORLEVEL% neq 0 ( 
              exit /b %ERRORLEVEL%
          )
 
      - name: Patch .lib files
        if: steps.cache-libs.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
          $libPath = "${env:INSTALL_DIR}\opus\${{ matrix.arch }}\lib"
          cp -v "$libPath\opus.lib" "$libPath\libopus.a"
      
      - name: List generated files
        if: steps.cache-libs.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
          Get-ChildItem "${env:INSTALL_DIR}\opus" -Recurse

      - name: Upload Opus artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: opus-win-${{ matrix.arch }}
          path: |
            install/opus/${{ matrix.arch }}/**/*
     
  pjsip-win:
    name: pjsip - win (${{ matrix.arch }})
    runs-on: windows-2022
    needs: [bcg729-win, openh264-win, openssl-win, opus-win]
    strategy:
      fail-fast: false
      matrix:
        arch: [x64, arm64]
    steps: 
      - uses: actions/checkout@v6

      - name: Cache pjsip Built SDK
        id: cache-libs
        uses: actions/cache@v5
        with:
          path: ${{ github.workspace }}\src\libpjsua2.win\*
          key: pjsip-${{ env.PJSIP_VERSION }}-win-${{ matrix.arch }}

      - name: Setup MSVC
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: ${{ matrix.arch }}
        if: steps.cache-libs.outputs.cache-hit != 'true'

      - name: Setup NASM
        uses: ilammy/setup-nasm@v1
        if: steps.cache-libs.outputs.cache-hit != 'true'
             
      - name: Validate SWIG
        shell: pwsh
        run: |
          swig -version
        if: steps.cache-libs.outputs.cache-hit != 'true'

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
        if: steps.cache-libs.outputs.cache-hit != 'true'

      - name: Setup CMake
        uses: lukka/get-cmake@latest
        if: steps.cache-libs.outputs.cache-hit != 'true'

      - name: Create directories
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path "${env:NUGET_ARTIFACTS}" | Out-Null
          New-Item -ItemType Directory -Force -Path "${env:LOGS_DIR}" | Out-Null
          New-Item -ItemType Directory -Force -Path "${env:INSTALL_DIR}" | Out-Null
        if: steps.cache-libs.outputs.cache-hit != 'true'

      - name: Download OpenSSL
        uses: actions/download-artifact@v4
        with:
          name: openssl-win-${{ matrix.arch == 'x64' && 'x64' || 'ARM64' }}
          path: deps/openssl
        if: steps.cache-libs.outputs.cache-hit != 'true'

      - name: Download Opus
        uses: actions/download-artifact@v4
        with:
          name: opus-win-${{ matrix.arch == 'x64' && 'x64' || 'ARM64' }}
          path: deps/opus
        if: steps.cache-libs.outputs.cache-hit != 'true'
   
      - name: Download BCG729
        uses: actions/download-artifact@v4
        with:
          name: bcg729-win-${{ matrix.arch }}
          path: deps/bcg729
        if: steps.cache-libs.outputs.cache-hit != 'true'

      - name: Download OpenH264
        uses: actions/download-artifact@v4
        with:
          name: openh264-win-${{ matrix.arch == 'x64' && 'x64' || 'ARM64' }}
          path: deps/openh264
        if: steps.cache-libs.outputs.cache-hit != 'true'
  
      - name: Validate Deps
        if: steps.cache-libs.outputs.cache-hit != 'true'
        working-directory: deps
        shell: pwsh
        run: |
            Get-ChildItem . -Recurse

      - name: Clone PJSIP
        shell: pwsh
        run: |
          git clone --branch "${env:PJSIP_VERSION}" --depth 1 https://github.com/pjsip/pjproject.git pjproject
        if: steps.cache-libs.outputs.cache-hit != 'true'

      - name: Create config_site.h
        shell: pwsh
        run: |
          $config = @"
          #include <pj/config_site_sample.h>
          #define PJ_HAS_IPV6                       1
          #define PJ_HAS_SSL_SOCK                   1
          #define PJ_SSL_SOCK_IMP                   PJ_SSL_SOCK_IMP_OPENSSL
          #define PJSIP_HAS_TLS_TRANSPORT           1
          #define PJMEDIA_AUDIO_DEV_HAS_PORTAUDIO   0
          #define PJMEDIA_HAS_VIDEO                 1
          #define PJMEDIA_HAS_OPENH264_CODEC        1
          #define PJMEDIA_HAS_OPUS_CODEC            1
          #define PJMEDIA_HAS_BCG729                1
          #define PJ_DLL                            1
          #define PJ_EXPORTING                      1
          #define PJ_EXPORT_SPECIFIER               __declspec(dllexport)
          #define PJ_IMPORT_SPECIFIER               __declspec(dllimport)

          "@

          New-Item -ItemType Directory -Force -Path "pjproject/pjlib/include/pj" | Out-Null
          Set-Content -Path "pjproject/pjlib/include/pj/config_site.h" -Value $config
        if: steps.cache-libs.outputs.cache-hit != 'true'

      - name: Patch SWIG C# vcxproj linker settings
        working-directory: pjproject
        shell: pwsh
        if: steps.cache-libs.outputs.cache-hit != 'true'
        run: |
          $projects = Get-ChildItem -Recurse -Filter *.vcxproj

          foreach ($proj in $projects) {
              Write-Host "Patching $($proj.FullName)"

              [xml]$xml = Get-Content $proj.FullName

              $ns = New-Object System.Xml.XmlNamespaceManager($xml.NameTable)
              $ns.AddNamespace("msb", "http://schemas.microsoft.com/developer/msbuild/2003")

              $links = $xml.SelectNodes("//msb:ItemDefinitionGroup/msb:Link", $ns)

              foreach ($link in $links) {

                  #
                  # Remove legacy duplicate OpenSSL libs
                  #
                  $depsNode = $link.SelectSingleNode("msb:AdditionalDependencies", $ns)

                  if ($depsNode -ne $null) {

                      $deps = $depsNode.InnerText.Split(';') |
                          ForEach-Object { $_.Trim() } |
                          Where-Object { $_ -ne "" }

                      $filtered = @()
                      $hasModernOpenSsl = $false

                      foreach ($d in $deps) {
                          if ($d -ieq "libssl.lib" -or $d -ieq "libcrypto.lib") {
                              $hasModernOpenSsl = $true
                          }
                      }

                      foreach ($d in $deps) {

                          #
                          # Remove duplicate legacy ssl/crypto
                          #
                          if ($hasModernOpenSsl -and (
                              $d -ieq "ssl.lib" -or
                              $d -ieq "crypto.lib"
                          )) {
                              Write-Host "Removing duplicate dependency: $d"
                              continue
                          }

                          #
                          # Avoid duplicated entries
                          #
                          if ($filtered -notcontains $d) {
                              $filtered += $d
                          }
                      }

                      $depsNode.InnerText = ($filtered -join ';')
                  }

                  #
                  # Remove IgnoreSpecificDefaultLibraries
                  #
                  $ignoreNode = $link.SelectSingleNode("msb:IgnoreSpecificDefaultLibraries", $ns)

                  if ($ignoreNode -ne $null) {
                      Write-Host "Removing IgnoreSpecificDefaultLibraries"
                      $link.RemoveChild($ignoreNode) | Out-Null
                  }

                  #
                  # Remove NODEFAULTLIB from AdditionalOptions
                  #
                  $optsNode = $link.SelectSingleNode("msb:AdditionalOptions", $ns)

                  if ($optsNode -ne $null) {

                      $opts = $optsNode.InnerText

                      $opts = $opts -replace '/NODEFAULTLIB:msvcrt\.lib', ''
                      $opts = $opts -replace '/NODEFAULTLIB', ''

                      $opts = ($opts -split '\s+' | Where-Object { $_ -ne "" }) -join ' '

                      $optsNode.InnerText = $opts
                  }
              }

              $xml.Save($proj.FullName)
          }
 
      - name: Retarget Windows SDK
        if: steps.cache-libs.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
            $projects = Get-ChildItem pjproject -Recurse -Filter *.vcxproj

            foreach ($proj in $projects)
            {
                Write-Host "Retargeting $($proj.FullName)"

                [xml]$xml = Get-Content $proj

                $ns = New-Object System.Xml.XmlNamespaceManager($xml.NameTable)
                $ns.AddNamespace("ns", "http://schemas.microsoft.com/developer/msbuild/2003")

                $nodes = $xml.SelectNodes("//ns:WindowsTargetPlatformVersion", $ns)

                foreach ($node in $nodes)
                {
                    $node.InnerText = "10.0"
                }

                $xml.Save($proj.FullName)
            }

      - name: Force Static Runtime (MT) Alignment across Architectures
        if: steps.cache-libs.outputs.cache-hit != 'true' && matrix.arch == 'arm64'
        shell: pwsh
        run: |
            $projects = Get-ChildItem pjproject -Recurse -Filter *.vcxproj

            foreach ($proj in $projects)
            {
                Write-Host "Forcing /MT Runtime on $($proj.FullName)"

                [xml]$xml = Get-Content $proj.FullName

                $ns = New-Object System.Xml.XmlNamespaceManager($xml.NameTable)
                $ns.AddNamespace("ns", "http://schemas.microsoft.com/developer/msbuild/2003")

                # Localiza todas as tags ClCompile dentro de configurações de Release
                $clCompiles = $xml.SelectNodes("//ns:ItemDefinitionGroup[contains(@Condition, 'Release')]/ns:ClCompile", $ns)

                foreach ($cc in $clCompiles)
                {
                    $rtNode = $cc.SelectSingleNode("ns:RuntimeLibrary", $ns)

                    if ($null -eq $rtNode)
                    {
                        # Se não existir a tag, cria e injeta MultiThreaded (/MT)
                        $rtNode = $xml.CreateElement("RuntimeLibrary", $xml.Project.NamespaceURI)
                        $cc.AppendChild($rtNode) | Out-Null
                    }
                    
                    # Força o valor para MultiThreaded (Equivalente a /MT para Release)
                    if ($rtNode.InnerText -ne "MultiThreaded")
                    {
                        Write-Host "  -> Changing RuntimeLibrary from '$($rtNode.InnerText)' to 'MultiThreaded'"
                        $rtNode.InnerText = "MultiThreaded"
                    }
                }

                $xml.Save($proj.FullName)
            }

      - name: Enable bigobj for SWIG wrapper
        if: steps.cache-libs.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
            $proj = "pjproject\pjsip-apps\build\swig_csharp_pjsua2.vcxproj"

            [xml]$xml = Get-Content $proj

            $ns = New-Object System.Xml.XmlNamespaceManager($xml.NameTable)
            $ns.AddNamespace("ns", "http://schemas.microsoft.com/developer/msbuild/2003")

            $nodes = $xml.SelectNodes("//ns:ClCompile", $ns)

            foreach ($node in $nodes)
            {
                $optNode = $node.SelectSingleNode("ns:AdditionalOptions", $ns)

                if ($null -eq $optNode)
                {
                    $optNode = $xml.CreateElement("AdditionalOptions", $xml.Project.NamespaceURI)
                    $node.AppendChild($optNode) | Out-Null
                }

                if ($optNode.InnerText -notmatch "/bigobj")
                {
                    $optNode.InnerText = "/bigobj %(AdditionalOptions)"
                }
            }

            $xml.Save($proj)            
  
      - name: Patch SWIG linker settings
        if: steps.cache-libs.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
            $proj = "pjproject\pjsip-apps\build\swig_csharp_pjsua2.vcxproj"

            [xml]$xml = Get-Content $proj

            $ns = New-Object System.Xml.XmlNamespaceManager($xml.NameTable)
            $ns.AddNamespace("ns", "http://schemas.microsoft.com/developer/msbuild/2003")

            $links = $xml.SelectNodes("//ns:Link", $ns)

            foreach ($link in $links)
            {
                $deps = $link.SelectSingleNode("ns:AdditionalDependencies", $ns)

                if ($null -eq $deps)
                {
                    $deps = $xml.CreateElement("AdditionalDependencies", $xml.Project.NamespaceURI)
                    $link.AppendChild($deps) | Out-Null
                }

                $deps.InnerText = @(
                    "opus.lib",
                    "bcg729.lib",
                    "openh264.lib",
                    "ssl.lib",
                    "crypto.lib",
                    "ws2_32.lib",
                    "crypt32.lib",
                    "iphlpapi.lib",
                    "secur32.lib",
                    "%(AdditionalDependencies)"
                ) -join ";"
            }

            $xml.Save($proj)

      - name: Force bcg729 static headers
        if: steps.cache-libs.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
            $headers = @(
                "deps/bcg729/include/bcg729/encoder.h",
                "deps/bcg729/include/bcg729/decoder.h"
            )

            foreach ($header in $headers)
            {
                Write-Host "Patching $header"

                $content = Get-Content $header -Raw

                $content = $content -replace `
                    "#define BCG729_VISIBILITY __declspec\(dllimport\)", `
                    "#define BCG729_VISIBILITY"

                $content = $content -replace `
                    "#define BCG729_VISIBILITY __declspec\(dllexport\)", `
                    "#define BCG729_VISIBILITY"

                Set-Content $header $content
            }            

      - name: Patch c# namespace
        if: steps.cache-libs.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
            $projectFile = "pjproject\pjsip-apps\build\swig_csharp_pjsua2.vcxproj"

            $oldNs = 'pjsua2xamarin.pjsua2'
            $newNs = 'libpjsua2.maui'

            (Get-Content $projectFile) `
            -replace [regex]::Escape($oldNs), $newNs |
            Set-Content $projectFile
            
      - name: Patch Windows SDK Tool Path for ARM64 Cross-Compile
        if: steps.cache-libs.outputs.cache-hit != 'true' && matrix.arch == 'arm64'
        shell: pwsh
        working-directory: pjproject
        run: |
          Write-Host "=== Fixing Windows SDK Tool Paths for ARM64 Cross-Compilation ==="
          
          # 1. Copiar fisicamente o rc.exe e rcdll.dll para a pasta de ferramentas do MSVC para garantir o fallback
          $MsvcBinDir = $env:VCINSTALLDIR + "Tools\MSVC\" + $env:VCToolsVersion + "\bin\HostX64\arm64"
          $SdkX64BinDir = $env:WindowsSdkDir + "bin\" + $env:WindowsSDKVersion + "x64"
          
          Write-Host "Copying rc.exe and rcdll.dll from SDK x64 to MSVC HostX64/arm64..."
          if (Test-Path "$SdkX64BinDir\rc.exe") {
              Copy-Item "$SdkX64BinDir\rc.exe" -Destination "$MsvcBinDir\" -Force
              Copy-Item "$SdkX64BinDir\rcdll.dll" -Destination "$MsvcBinDir\" -Force
              Write-Host "  -> Files successfully copied to MSVC Toolset directory."
          } else {
              Write-Warning "  -> Could not locate rc.exe in $SdkX64BinDir"
          }

          # 2. Injetar propriedades de infraestrutura do SDK no arquivo .vcxproj
          $ProjPath = "pjsip-apps\build\swig_csharp_pjsua2.vcxproj"
          Write-Host "Injecting SDK path descriptors directly into $ProjPath"

          [xml]$xml = Get-Content $ProjPath
          $ns = New-Object System.Xml.XmlNamespaceManager($xml.NameTable)
          $ns.AddNamespace("ns", "http://schemas.microsoft.com/developer/msbuild/2003")

          # Encontra o primeiro PropertyGroup global (geralmente o primeiro filho do nó raiz Project)
          $propGroup = $xml.SelectSingleNode("//ns:PropertyGroup[not(@Condition)]", $ns)
          if ($null -eq $propGroup) {
              $propGroup = $xml.SelectSingleNode("//ns:PropertyGroup", $ns)
          }

          if ($null -ne $propGroup) {
              # Injeta explicitamente o caminho das ferramentas de build para apontar para a pasta HostX64
              $rcToolPathNode = $xml.CreateElement("RcToolPath", $xml.Project.NamespaceURI)
              $rcToolPathNode.InnerText = "$SdkX64BinDir"
              $propGroup.AppendChild($rcToolPathNode) | Out-Null

              # Injeta o subdiretório correto do compilador de recursos
              $rcPathNode = $xml.CreateElement("ResourceCompilerToolPath", $xml.Project.NamespaceURI)
              $rcPathNode.InnerText = "$SdkX64BinDir"
              $propGroup.AppendChild($rcPathNode) | Out-Null

              $xml.Save($ProjPath)
              Write-Host "  -> XML successfully patched with RcToolPath."
          } else {
              Write-Error "  -> Target PropertyGroup could not be found in vcxproj XML structure."
          }

      - name: Build 
        shell: pwsh
        if: steps.cache-libs.outputs.cache-hit != 'true'
        working-directory: pjproject
        run: |
          # Include Path Mapping from third party libraries
          $DepsDir    = "${env:GITHUB_WORKSPACE}/deps"
          $OpusLib    = "${DepsDir}/opus/lib"
          $Bcg729Lib  = "${DepsDir}/bcg729/lib"
          $H264Lib    = "${DepsDir}/openh264/lib"
          $SslLib     = "${DepsDir}/openssl/lib"
          $OpusInc    = "${DepsDir}/opus/include"
          $Bcg729Inc  = "${DepsDir}/bcg729/include"
          $H264Inc    = "${DepsDir}/openh264/include"
          $SslInc     = "${DepsDir}/openssl/include"

          Write-Host "=== Windows Native SDK Infrastructure Mapping ==="
          $SdkInc     = $env:WindowsSdkDir + "Include\" + $env:WindowsSDKVersion
          $UcrtInc    = "${SdkInc}\ucrt"
          $SharedInc  = "${SdkInc}\shared"
          $UmInc      = "${SdkInc}\um"
          $MsvcInc    = $env:VCINSTALLDIR + "Tools\MSVC\" + $env:VCToolsVersion + "\include"

          # INCLUDE setup
          $IncludePaths = @($MsvcInc, $UcrtInc, $SharedInc, $UmInc, $OpusInc, $Bcg729Inc, $H264Inc, $SslInc)
          if ($env:INCLUDE) { $IncludePaths += $env:INCLUDE }
          $env:INCLUDE = $IncludePaths -join ";"
          
          # Link Libraries Folder Mapping (.lib) adaptado dinamicamente para a arquitetura alvo
          $TargetArch = "${{ matrix.arch }}"
          $SdkLib     = $env:WindowsSdkDir + "Lib\" + $env:WindowsSDKVersion
          $UcrtLib    = "${SdkLib}\ucrt\${TargetArch}"
          $UmLib      = "${SdkLib}\um\${TargetArch}"
          $MsvcLib    = $env:VCINSTALLDIR + "Tools\MSVC\" + $env:VCToolsVersion + "\lib\${TargetArch}"

          $LibPaths = @($OpusLib, $Bcg729Lib, $H264Lib, $SslLib, $MsvcLib, $UcrtLib, $UmLib)
          if ($env:LIB) { $LibPaths += $env:LIB }
          $env:LIB = $LibPaths -join ";"

          # === CORREÇÃO PARA O TRK0005 (CL.EXE) & LNK1158 (RC.EXE) ===
          $MsvcBinDir = $env:VCINSTALLDIR + "Tools\MSVC\" + $env:VCToolsVersion + "\bin\HostX64\${TargetArch}"
          $SdkBinDir  = $env:WindowsSdkDir + "bin\" + $env:WindowsSDKVersion + "${TargetArch}"
          
          # SOLUÇÃO PARA O RC.EXE EM ARM64:
          # O rc.exe não existe na pasta 'arm64' do SDK, ele roda a partir da pasta 'x64' (Host).
          # Adicionamos a pasta x64 do SDK como fallback de ferramentas.
          $SdkHostBinDir = $env:WindowsSdkDir + "bin\" + $env:WindowsSDKVersion + "x64"

          if ($TargetArch -eq "arm64") {
              $env:PATH = "${MsvcBinDir};${SdkBinDir};${SdkHostBinDir};${env:PATH}"
              $CustomExecPath = "`"${MsvcBinDir}`"%3b`"${SdkBinDir}`"%3b`"${SdkHostBinDir}`"%3b$env:PATH"
          } else {
              $env:PATH = "${MsvcBinDir};${SdkBinDir};${env:PATH}"
              $CustomExecPath = "`"${MsvcBinDir}`"%3b`"${SdkBinDir}`"%3b$env:PATH"
          }

          Write-Host "=== Building with environment variables ==="
          $MsBuildPlatform = if ($TargetArch -eq "arm64") { "ARM64" } else { "x64" }
          Write-Host "=== Building pjsip for win-${{ matrix.arch }} ==="

          $TargetSdkVersion = $env:WindowsSDKVersion.TrimEnd('\')

          msbuild pjsip-apps\build\swig_csharp_pjsua2.vcxproj `
              /p:PlatformToolset=v143 `
              /p:Configuration=Release `
              /p:Platform=$MsBuildPlatform `
              /p:WindowsTargetPlatformVersion="$TargetSdkVersion" `
              /p:AdditionalOptions="/bigobj" `
              /p:ExecutablePath=$CustomExecPath `
              /p:UseEnv=true
      
      - name: Collect PJSIP artifacts
        if: steps.cache-libs.outputs.cache-hit != 'true'
        working-directory: src
        shell: pwsh
        run: | 
          New-Item -ItemType Directory -Force -Path "${{ github.workspace }}\src\libpjsua2.win\pjsua2" | Out-Null
          New-Item -ItemType Directory -Force -Path "${{ github.workspace }}\src\libpjsua2.win\lib\${{ matrix.arch }}" | Out-Null

          if (Test-Path "${{ github.workspace }}\pjproject\pjsip-apps\src\swig\csharp\output\") {
            Write-Host "Copying pjsip dll"
            Copy-Item ${{ github.workspace }}\pjproject\pjsip-apps\src\swig\csharp\output\pjsua2.dll ".\libpjsua2.win\lib\${{ matrix.arch }}" -Recurse -Force -Verbose
            Write-Host "Copying pjsip managed c# classes"
            Copy-Item ${{ github.workspace }}\pjproject\pjsip-apps\src\swig\csharp\output\*.cs ".\libpjsua2.win\pjsua2" -Verbose
          }
          
          Get-ChildItem . -Recurse
          

      - name: Upload PJSIP artifacts
        uses: actions/upload-artifact@v4
        with:
          name: pjsip-${{ env.PJSIP_VERSION }}-Win-${{ matrix.arch }}
          path: ${{ github.workspace }}\src\libpjsua2.win\*
          if-no-files-found: warn
 
  synchronize-swig-sources:
    name: Synchronize generated SWIG sources
    runs-on: ubuntu-latest
    needs: [pjsip-android, pjsip-ios, pjsip-mac, pjsip-win]
    if: github.event_name == 'pull_request'
    permissions:
      contents: write
      actions: read

    steps:
      - name: Checkout PR branch
        uses: actions/checkout@v6
        with:
          ref: ${{ github.head_ref }}
      
      - name: Download Android x86 Artifact
        uses: actions/download-artifact@v8
        with:
          name: pjsip-${{ env.PJSIP_VERSION }}-android-x86
          path: ${{ env.ARTIFACTS }}/pjsip-${{ env.PJSIP_VERSION }}-android-x86
      
      - name: Download ios Artifacts
        uses: actions/download-artifact@v8
        with:
          name: pjsip-${{ env.PJSIP_VERSION }}-iPhoneSimulator${{ env.SDKVERSION }}-x86_64.sdk
          path: ${{ env.ARTIFACTS }}/pjsip-${{ env.PJSIP_VERSION }}-iPhoneSimulator${{ env.SDKVERSION }}-x86_64.sdk
    
      - uses: actions/download-artifact@v8
        name: Download pjsip macos (x64)
        with:
          name: pjsip-${{env.PJSIP_VERSION}}-MacOSX${{ env.SDKVERSION }}-x86_64.sdk
          path: ${{ env.ARTIFACTS }}/pjsip-${{env.PJSIP_VERSION}}-MacOSX${{ env.SDKVERSION }}-x86_64.sdk
 
      - name: Download win x64 Artifact
        uses: actions/download-artifact@v8
        with:
          name: pjsip-${{ env.PJSIP_VERSION }}-Win-x64
          path: ${{ env.ARTIFACTS }}/pjsip-${{ env.PJSIP_VERSION }}-Win-x64
    
      - name: Prepare Dirs
        run: |
          mkdir -p ${{ github.workspace }}/src/libpjsua2maui.android/pjsua2maui/pjsua2
          mkdir -p ${{ github.workspace }}/src/libpjsua2.android.so/org/pjsip
          mkdir -p ${{ github.workspace }}/src/libpjsua2maui.ios/pjsua2maui/pjsua2
          mkdir -p ${{ github.workspace }}/src/libpjsua2maui.mac/pjsua2maui/pjsua2
          mkdir -p ${{ github.workspace }}/src/libpjsua2maui.win/pjsua2maui/pjsua2

          # Move managed classes to projects directory
          cp -v ${{ env.ARTIFACTS }}/pjsip-${{ env.PJSIP_VERSION }}-android-x86/java/*.java ${{ github.workspace }}/src/libpjsua2.android.so/org/pjsip
          cp -v ${{ env.ARTIFACTS }}/pjsip-${{ env.PJSIP_VERSION }}-android-x86/csharp/*.cs ${{ github.workspace }}/src/libpjsua2maui.android/pjsua2maui/pjsua2
          cp -v ${{ env.ARTIFACTS }}/pjsip-${{ env.PJSIP_VERSION }}-iPhoneOS${{ env.SDKVERSION }}-x86_64.sdk/cs/*.cs ${{ github.workspace }}/src/libpjsua2maui.ios/pjsua2maui/pjsua2
          cp -v ${{ env.ARTIFACTS }}/pjsip-${{ env.PJSIP_VERSION }}-MacOSX${{ env.SDKVERSION }}-x86_64.sdk/cs/*.cs ${{ github.workspace }}/src/libpjsua2maui.mac/pjsua2maui/pjsua2
          cp -v ${{ env.ARTIFACTS }}/pjsip-${{ env.PJSIP_VERSION }}-Win-x64/pjsua2/*.cs ${{ github.workspace }}/src/libpjsua2maui.win/pjsua2maui/pjsua2

      - name: Synchronize generated SWIG sources
        shell: bash
        run: |
            set -e
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            echo "Adding generated C# files..."
            git add \
              src/libpjsua2.android.so/org/pjsip/*.java \
              src/libpjsua2maui.android/pjsua2maui/pjsua2/*.cs \
              src/libpjsua2maui.ios/pjsua2maui/pjsua2/*.cs \
              src/libpjsua2maui.mac/pjsua2maui/pjsua2/*.cs \
              src/libpjsua2maui.win/pjsua2maui/pjsua2/*.cs
            if git diff --cached --quiet; then
              echo "No generated sources changed."
            else
              git commit -m "feat: Update generated SWIG bindings"
              git push origin HEAD:${{ github.head_ref }}
              echo "Generated bindings committed successfully."
              exit 0
            fi
  nupkg:
    runs-on: macos-26
    name: libpjsua2.nupkg
    needs: [synchronize-swig-sources]
    permissions:
      id-token: write
      contents: write
      packages: write
      actions: read

    steps: 
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set VERSION variable
        run: |
          NUSPEC_PATH="${{ github.workspace }}/src/libpjsua2maui.nuspec"
          
          VERSION=$(xmllint --xpath "//*[local-name()='version']/text()" "$NUSPEC_PATH" 2>/dev/null | xargs)
          
          if [ -n "$VERSION" ]; then
            echo "Found version: $VERSION"
            echo "VERSION=$VERSION" >> $GITHUB_ENV
          else
            echo "Error: Tag <version> not found at .nuspec file"
            exit 1
          fi

      - name: Cache pjsip Built SDK
        uses: actions/cache@v5
        id: cache-libs
        with:
          path: ${{ github.workspace }}/src/*.nupkg
          key: libpjsua2maui${{ env.VERSION }}.nupkg-${{ env.PJSIP_VERSION }}
        env: 
          VERSION: ${{ env.VERSION }}

      - name: Setup .NET 10
        if: steps.cache-libs.outputs.cache-hit != 'true'
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.0.x'
          
      - name: Setup nuget 
        run: brew install nuget
        if: steps.cache-libs.outputs.cache-hit != 'true'
 
      - uses: actions/setup-java@v5
        with:
          distribution: 'microsoft'
          java-version: '17'
        if: steps.cache-libs.outputs.cache-hit != 'true'
          
      - name: Download Android x86 Artifact
        uses: actions/download-artifact@v8
        with:
          name: pjsip-${{ env.PJSIP_VERSION }}-android-x86
          path: ${{ env.ARTIFACTS }}/pjsip-${{ env.PJSIP_VERSION }}-android-x86
        if: steps.cache-libs.outputs.cache-hit != 'true'
      
      - name: Download Android x86_64 Artifact
        uses: actions/download-artifact@v8
        with:
          name: pjsip-${{ env.PJSIP_VERSION }}-android-x86_64
          path: ${{ env.ARTIFACTS }}/pjsip-${{ env.PJSIP_VERSION }}-android-x86_64
        if: steps.cache-libs.outputs.cache-hit != 'true'
          
      - name: Download Android arm Artifact
        uses: actions/download-artifact@v8
        with:
          name: pjsip-${{ env.PJSIP_VERSION }}-android-armeabi-v7a
          path: ${{ env.ARTIFACTS }}/pjsip-${{ env.PJSIP_VERSION }}-android-armeabi-v7a
        if: steps.cache-libs.outputs.cache-hit != 'true'
      
      - name: Download Android arm64 Artifact
        uses: actions/download-artifact@v8
        with:
          name: pjsip-${{ env.PJSIP_VERSION }}-android-arm64-v8a
          path: ${{ env.ARTIFACTS }}/pjsip-${{ env.PJSIP_VERSION }}-android-arm64-v8a
        if: steps.cache-libs.outputs.cache-hit != 'true'
  
      - name: Download ios Artifacts
        uses: actions/download-artifact@v8
        with:
          name: pjsip-${{ env.PJSIP_VERSION }}-iPhoneSimulator${{ env.SDKVERSION }}-x86_64.sdk
          path: ${{ env.ARTIFACTS }}/pjsip-${{ env.PJSIP_VERSION }}-iPhoneSimulator${{ env.SDKVERSION }}-x86_64.sdk
        if: steps.cache-libs.outputs.cache-hit != 'true'
      
      - name: Download ios arm64 Artifact
        uses: actions/download-artifact@v8
        with:
          name: pjsip-${{ env.PJSIP_VERSION }}-iPhoneOS${{ env.SDKVERSION }}-arm64.sdk
          path: ${{ env.ARTIFACTS }}/pjsip-${{ env.PJSIP_VERSION }}-iPhoneOS${{ env.SDKVERSION }}-arm64.sdk
        if: steps.cache-libs.outputs.cache-hit != 'true'

      - name: Download ios arm64s Artifact
        uses: actions/download-artifact@v8
        with:
          name: pjsip-${{ env.PJSIP_VERSION }}-iPhoneSimulator${{ env.SDKVERSION }}-arm64s.sdk
          path: ${{ env.ARTIFACTS }}/pjsip-${{ env.PJSIP_VERSION }}-iPhoneSimulator26.2-arm64s.sdk
        if: steps.cache-libs.outputs.cache-hit != 'true'

      - uses: actions/download-artifact@v8
        name: Download pjsip macos (arm64)
        with:
          name: pjsip-${{env.PJSIP_VERSION}}-MacOSX${{ env.SDKVERSION }}-arm64.sdk
          path: ${{ env.ARTIFACTS }}/pjsip-${{env.PJSIP_VERSION}}-MacOSX${{ env.SDKVERSION }}-arm64.sdk
        if: steps.cache-libs.outputs.cache-hit != 'true'

      - uses: actions/download-artifact@v8
        name: Download pjsip macos (x64)
        with:
          name: pjsip-${{env.PJSIP_VERSION}}-MacOSX${{ env.SDKVERSION }}-x86_64.sdk
          path: ${{ env.ARTIFACTS }}/pjsip-${{env.PJSIP_VERSION}}-MacOSX${{ env.SDKVERSION }}-x86_64.sdk
        if: steps.cache-libs.outputs.cache-hit != 'true'

      - name: Download win ARM64 Artifact
        uses: actions/download-artifact@v8
        with:
          name: pjsip-${{ env.PJSIP_VERSION }}-Win-arm64
          path: ${{ env.ARTIFACTS }}/pjsip-${{ env.PJSIP_VERSION }}-Win-arm64
        if: steps.cache-libs.outputs.cache-hit != 'true'
      
      - name: Download win x64 Artifact
        uses: actions/download-artifact@v8
        with:
          name: pjsip-${{ env.PJSIP_VERSION }}-Win-x64
          path: ${{ env.ARTIFACTS }}/pjsip-${{ env.PJSIP_VERSION }}-Win-x64
        if: steps.cache-libs.outputs.cache-hit != 'true'
      
      - name: Validate artifacts
        if: steps.cache-libs.outputs.cache-hit != 'true'
        run: |
          ls -R ${{ env.ARTIFACTS }}

      - name: Prepare Dirs
        if: steps.cache-libs.outputs.cache-hit != 'true'
        run: |
          mkdir -p ${{ github.workspace }}/src/libpjsua2.win/bin/arm64
          mkdir -p ${{ github.workspace }}/src/libpjsua2.win/bin/x64
          mkdir -p ${{ github.workspace }}/src/libpjsua2.android.so/lib/x86
          mkdir -p ${{ github.workspace }}/src/libpjsua2.android.so/lib/x86_64
          mkdir -p ${{ github.workspace }}/src/libpjsua2.android.so/lib/armeabi-v7a
          mkdir -p ${{ github.workspace }}/src/libpjsua2.android.so/lib/arm64-v8a
          mkdir -p ${{ github.workspace }}/src/libpjsua2.android.so/lib/org/pjsip
          mkdir -p ${{ github.workspace }}/src/libpjsua2.ios.a/lib/arm64
          mkdir -p ${{ github.workspace }}/src/libpjsua2.ios.a/lib/arm64s
          mkdir -p ${{ github.workspace }}/src/libpjsua2.ios.a/lib/x86_64
          mkdir -p ${{ github.workspace }}/src/libpjsua2.mac.a/lib/arm64
          mkdir -p ${{ github.workspace }}/src/libpjsua2.mac.a/lib/x86_64
          mkdir -p ${{ github.workspace }}/src/libpjsua2.win/bin/x64/ 
          mkdir -p ${{ github.workspace }}/src/libpjsua2.win/bin/arm64/ 

          # Move android libs to packaging directory
          cp -v ${{ env.ARTIFACTS }}/pjsip-${{ env.PJSIP_VERSION }}-android-x86/lib/lib*.so ${{ github.workspace }}/src/libpjsua2.android.so/lib/x86
          cp -v ${{ env.ARTIFACTS }}/pjsip-${{ env.PJSIP_VERSION }}-android-x86_64/lib/lib*.so ${{ github.workspace }}/src/libpjsua2.android.so/lib/x86_64
          cp -v ${{ env.ARTIFACTS }}/pjsip-${{ env.PJSIP_VERSION }}-android-armeabi-v7a/lib/lib*.so ${{ github.workspace }}/src/libpjsua2.android.so/lib/armeabi-v7a
          cp -v ${{ env.ARTIFACTS }}/pjsip-${{ env.PJSIP_VERSION }}-android-arm64-v8a/lib/lib*.so ${{ github.workspace }}/src/libpjsua2.android.so/lib/arm64-v8a
          
          # Move ios libs to packaging directory
          cp -v ${{ env.ARTIFACTS }}/pjsip-${{ env.PJSIP_VERSION }}-iPhoneOS26.2-arm64.sdk/lib/* ${{ github.workspace }}/src/libpjsua2.ios.a/lib/x86_64
          cp -v ${{ env.ARTIFACTS }}/pjsip-${{ env.PJSIP_VERSION }}-iPhoneSimulator26.2-arm64s.sdk/lib/* ${{ github.workspace }}/src/libpjsua2.ios.a/lib/arm64s
          cp -v ${{ env.ARTIFACTS }}/pjsip-${{ env.PJSIP_VERSION }}-iPhoneSimulator26.2-x86_64.sdk/lib/* ${{ github.workspace }}/src/libpjsua2.ios.a/lib/arm64
          
          # Move mac libs to packaging directory
          cp -v ${{ env.ARTIFACTS }}/pjsip-${{ env.PJSIP_VERSION }}-MacOSX26.2-arm64.sdk/lib/* ${{ github.workspace }}/src/libpjsua2.mac.a/lib/arm64
          cp -v ${{ env.ARTIFACTS }}/pjsip-${{ env.PJSIP_VERSION }}-MacOSX26.2-x86_64.sdk/lib/* ${{ github.workspace }}/src/libpjsua2.mac.a/lib/x86_64
          
          # Move win libs to packaging directory
          cp -v ${{ env.ARTIFACTS }}/pjsip-${{ env.PJSIP_VERSION }}-Win-arm64/lib/arm64/*.dll ${{ github.workspace }}/src/libpjsua2.win/bin/arm64
          cp -v ${{ env.ARTIFACTS }}/pjsip-${{ env.PJSIP_VERSION }}-Win-x64/lib/x64/*.dll ${{ github.workspace }}/src/libpjsua2.win/bin/x64 
          
      - name: Restore Dependencies
        working-directory: src
        if: steps.cache-libs.outputs.cache-hit != 'true'
        run: | 
          echo "⏳ Installing required workloads for .NET 10..."
          dotnet workload install android ios maccatalyst --source https://api.nuget.org/v3/index.json --skip-manifest-update

          echo "⏳ Updating and synchronizing workloads..."
          dotnet workload update
          
          if [ -f "$(find . -name "*.slnx" -o -name "*.sln" | head -n 1)" ]; then
            echo "📦 Restoring entire solution..."
            dotnet workload restore $(find . -name "*.slnx" -o -name "*.sln" | head -n 1)
          fi

          echo "📦 Forcing clean NuGet restore for all target projects..."
          dotnet restore libpjsua2maui.android/libpjsua2maui.android.csproj
          dotnet restore libpjsua2maui.ios/libpjsua2maui.ios.csproj
          dotnet restore libpjsua2maui.mac/libpjsua2maui.mac.csproj
          dotnet restore libpjsua2maui.win/libpjsua2maui.win.csproj

      - name: Build MAUI Android
        if: steps.cache-libs.outputs.cache-hit != 'true'
        run: dotnet publish src/libpjsua2maui.android/libpjsua2maui.android.csproj -c Release -f net10.0-android -p:IsOfficialBuild=true --no-restore
        
      - name: Build MAUI iOS
        if: steps.cache-libs.outputs.cache-hit != 'true'
        run: dotnet publish src/libpjsua2maui.ios/libpjsua2maui.ios.csproj -c Release -f net10.0-ios -p:IsOfficialBuild=true --no-restore
      
      - name: Build MAUI MacOSX
        if: steps.cache-libs.outputs.cache-hit != 'true'
        run: dotnet publish src/libpjsua2maui.mac/libpjsua2maui.mac.csproj -c Release -f net10.0-maccatalyst -p:IsOfficialBuild=true --no-restore

      - name: Build MAUI Win
        if: steps.cache-libs.outputs.cache-hit != 'true'
        run: dotnet publish src/libpjsua2maui.win/libpjsua2maui.win.csproj -c Release -f net10.0-windows10.0.19041.0 -p:IsOfficialBuild=true --no-restore

      - name: NuGet login (OIDC) and package push
        id: login 
        if: startsWith(github.ref, 'refs/tags/')
        uses: NuGet/login@v1 
        with:
          user: ${{ env.USERNAME }}  
        env:
          USERNAME: ${{ secrets.USER_NAME }}

      - name: Pack NuGet Package
        if: steps.cache-libs.outputs.cache-hit != 'true'
        run: |
          cd ${{ github.workspace }}/src
          nuget pack libpjsua2maui.nuspec \
            -Version "${{ env.VERSION }}" \
            -BasePath "${{ github.workspace }}/src" \
            -NoPackageAnalysis \
            -NoDefaultExcludes \
            -Symbols \
            -SymbolPackageFormat snupkg
        env: 
          VERSION: ${{ env.VERSION }}
      
      - uses: actions/upload-artifact@v7
        if: steps.cache-libs.outputs.cache-hit != 'true'
        name: Upload NuGet packages
        with:
          name: libpjsua2maui.${{ env.VERSION }}
          path: |
            ${{ github.workspace }}/src/*.nupkg
            ${{ github.workspace }}/src/*.snupkg
        env: 
          VERSION: ${{ env.VERSION }}

      - name: Push to NuGet.org (OIDC / Key)
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          dotnet nuget push ${{ github.workspace }}/src/libpjsua2maui.${{ env.VERSION }}.nupkg \
            --api-key "${{ steps.login.outputs.NUGET_API_KEY }}" \
            --source https://api.nuget.org/v3/index.json \
            --skip-duplicate
        env: 
          VERSION: ${{ env.VERSION }}

      - name: Push symbols to NuGet.org
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          dotnet nuget push \
            ${{ github.workspace }}/src/libpjsua2maui.${{ env.VERSION }}.snupkg \
            --api-key "${{ steps.login.outputs.NUGET_API_KEY }}" \
            --source https://api.nuget.org/v3/index.json \
            --skip-duplicate
        env:
          VERSION: ${{ env.VERSION }}

      - name: Push to GitHub Packages
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          dotnet nuget push ${{ github.workspace }}/src/libpjsua2maui.${{ env.VERSION }}.nupkg \
            --source "https://nuget.pkg.github.com/${{ github.repository_owner }}/index.json" \
            --api-key "${{ secrets.GH_TOKEN }}" \
            --skip-duplicate
        env: 
          VERSION: ${{ env.VERSION }}
